# Finance — état de reprise

Mise à jour : 23 septembre 2026 (lots V2.1 à V2.8, correctifs PWA/actions de ligne, carte « Le mouvement du mois », section « Aperçu du mois », badges de nature de compte, la table « Mes comptes », la refonte visuelle générale — icônes, cartes, liste Abonnements, icônes de nature de compte, lignes de paiement colorées, hover des listes —, le tri « pas payé d'abord » + « Voir les autres mois », la simplification des lignes réglées + un audit qualité indépendant, un audit à sept agents (sécurité, calculs, patrimoine, design, import/migration, éditeur, couverture e2e), les dates lisibles du graphique de patrimoine, les coins plus carrés, les boutons payer/reçu colorés + refonte du sélecteur de mois + palette bleu néon, le correctif du focus clavier perdu après un règlement instantané, la refonte visuelle plate et neutre, puis la refonte « verre + bleu néon » — tous fusionnés dans `main`). Mise à jour : 24 septembre 2026 — thème « Midnight Glass » (noir graphite, verre fumé, accent unique bleu glacier `#A8BCE8`) appliqué à toute l'application, voir la section dédiée ; puis logo tirelire et installation iPhone/iPad/Windows, répétition « Tous les mois » à l'ajout, synchronisation chiffrée entre appareils par dépôt GitHub privé (sections dédiées). Ce fichier décrit les faits vérifiés. Le plan V2 décrit le travail suivant ; il ne constitue pas une preuve que ces améliorations sont déjà dans l’application au-delà de ce qui est explicitement marqué développé/testé ci-dessous.

## Lot V2.1 — Modèle récurrent (développé et testé le 18 septembre 2026)

`Recurrence` porte désormais un champ requis `recurrenceType` (`subscription`/`bill`/`income`/`saving`/`other`), cohérent avec `kind` (`income` exactement quand `kind === "income"`, validé). Un ancien coffre ou export version 1 est migré vers la version 2 de façon pure et idempotente (`src/domain/migration.ts`) avant la validation stricte : la classification `income` suit `kind`, `subscription` ne s’applique qu’à la catégorie exactement `Abonnements`, toute autre dépense migre en `other` — à vérifier, aucune classification n’est devinée. Une entrée déjà classée n’est jamais reclassée. L’enveloppe chiffrée du coffre (`vault.ts`) n’est pas touchée : sa version crypto reste un identifiant séparé de la version des données métier.

L’éditeur de récurrence a un nouveau champ « Nature », cohérent avec le type revenu/dépense choisi : basculer Type (Revenu ↔ Dépense) plusieurs fois avant d’enregistrer ne perd jamais un choix de Nature déjà fait côté dépense (`expenseRecurrenceType` distinct de l’affichage forcé « Revenu récurrent »).

Relu indépendamment par l’agent `finance-verification` (distinct de l’auteur), en deux passes. Défauts réels trouvés et corrigés avant que la PR ne sorte du brouillon :

1. La migration remplaçait silencieusement un champ `recurrences` corrompu (absent ou non tableau) par une liste vide au lieu de laisser la validation rejeter le fichier — corrigé, avec test de régression dédié.
2. Le champ « Nature » de l’éditeur de récurrence était un `<select>` non contrôlé dont les options changeaient avec Type : un aller-retour Dépense → Revenu → Dépense sans toucher à Nature perdait silencieusement un choix explicite (ex. « Charge ») au profit du premier choix de la nouvelle liste (« Abonnement »), y compris en modification d’une récurrence existante. Corrigé en dérivant l’affichage de deux états distincts (choix côté dépense conservé séparément, jamais écrasé par le forçage « Revenu récurrent ») ; scénario de non-régression ajouté dans `tests/finance.spec.ts` (bascule de Type puis relecture après enregistrement).

**Anomalie de processus notée par la relecture, corrigée pour la suite :** le premier commit de ce lot avait été publié (poussé, PR ouverte) avant que le verdict de la revue indépendante soit rendu, alors que le skill exige la relecture avant publication. La PR est restée en brouillon pendant toute la correction ; les deux défauts ci-dessus ont été corrigés et republiés avant toute demande de sortie de brouillon.

Preuves (recomptées après les deux corrections) : `pnpm run typecheck` (0 erreur), `pnpm run test` (**93/93**, 6 fichiers — un chiffre antérieur de 97/97 annoncé dans un commit précédent était faux, corrigé ici), `pnpm run build` (réussi), `pnpm audit --audit-level high` (aucune vulnérabilité). `pnpm run test:e2e` : les 4 scénarios passent rejoués individuellement (`--workers=1 -g "<nom>"`), y compris le nouveau scénario de non-régression Type/Nature ; en parallèle, Chromium single-process reste intermittent dans ce bac à sable — limite déjà documentée ci-dessous, pas une régression de ce lot. Code publié sur `claude/finance-app-completion-k86h7n`, PR #5 (brouillon) vers `main`.

**Toujours prévu, non développé :** V2.3 (page Abonnements), V2.5–V2.8 (cartes compactes, icônes/établissements, logo, livraison finale). V2.2 et V2.4 sont depuis complétés, voir plus bas. Aucune capture dédiée à ce lot : le champ « Nature » n’a pas modifié les captures existantes (tableau de bord, projets) au-delà d’un nouveau rendu du même jeu de données fictif.

## Lot V2.2 — Mois et statuts (complet) — développé et testé le 18 septembre 2026

Les trois volets du lot V2.2 sont traités ici : statuts payé/reçu explicites, sélecteur de mois français, et cohorte d’échéances.

Le bouton icône générique « Confirmer » est remplacé par une action texte sensible au type (« Marquer payé »/« Marquer reçu »/« Marquer réglé ») qui ouvre l’éditeur pré-rempli à `settled`/aujourd’hui au lieu d’écrire silencieusement au clic : la date de règlement reste visible et modifiable avant validation (`EditorSpec.transaction` préremplit le formulaire même pour une occurrence virtuelle pas encore persistée). Une correction « Remettre à payer/recevoir/régler », limitée aux occurrences liées à une récurrence, repasse le statut à prévu, restaure la date d’échéance et consigne l’ancienne date de règlement dans `source.note` au lieu de l’effacer (confirmation explicite requise ; identifiant de transaction inchangé, un reçu déjà joint reste lié). Les libellés « Payé »/« Reçu »/« Pas encore payé »/« Pas encore reçu » remplacent « Confirmé »/« Prévu » dans les lignes d’opération et le champ État de l’éditeur.

Relu indépendamment par l’agent `finance-verification` **avant tout push** (mais après un premier commit local — la relecture a noté que cette nuance de séquencement restait à corriger : commiter aussi après verdict, pas seulement différer le push). Deux défauts réels trouvés et corrigés avant publication :

1. `Editor.tsx` : la précédence de préremplissage était inversée (`data.transactions.find(...) ?? spec.transaction`) — l’ancien enregistrement persisté gagnait sur l’intention explicite de « Marquer payé » dès que la transaction existait déjà (le cas courant, pas seulement une occurrence jamais matérialisée). Corrigé (`spec.transaction ?? data.transactions.find(...)`) ; régression confirmée par contrôle négatif (le nouveau test e2e échoue sans le correctif, rejoué manuellement) puis test e2e dédié.
2. `index.css` : le nouveau bouton texte, plus large que l’ancienne icône seule, faisait s’effondrer `.row-main` à 390 px (une lettre par ligne, hauteur de page passée de 3711 px à 5602 px sur l’Accueil) car `min-width:0` empêchait tout déclenchement de retour à la ligne. Corrigé par un plancher de largeur sur `.row-main` et `flex-wrap` sur `.row` ; confirmé par inspection visuelle réelle des captures régénérées (390/834/1440 px, Accueil et Mon mois) — l’assertion automatisée `scrollWidth` existante ne détectait pas ce défaut (débordement vertical, pas horizontal).

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**93/93**, inchangé — ce lot est uniquement UI), `pnpm run build` (réussi), les 4 scénarios `test:e2e` rejoués individuellement, dont le nouveau scénario couvrant le cycle marquer payé → remettre à payer → marquer payé de nouveau (occurrence de récurrence et opération ponctuelle). Captures 390/834/1440 px inspectées visuellement après correction, pas seulement testées automatiquement.

### Sélecteur de mois français

Remplace l’ancien `<input type="month">` par `src/components/MonthPicker.tsx` : rangée compacte Janvier–Décembre, année dans un contrôle séparé (précédent/suivant), mois sélectionné mis en évidence, raccourci « Ce mois-ci » affiché seulement en dehors du mois courant. La valeur canonique `YYYY-MM` reste dans l’état existant (`month`/`setMonth`) ; le nom français n’est qu’une présentation, sans calendrier journalier. Libellés courts explicites (Jan/Fév/Mar/Avr/Mai/Jun/Jul/Aoû/Sep/Oct/Nov/Déc), pas un `slice(0,3)` naïf qui aurait rendu Juin et Juillet indiscernables (tous deux « Jui ») ; le nom complet reste le nom accessible (`aria-label`) de chaque bouton. La rangée défile sur les écrans étroits plutôt que de déborder ou de passer à la ligne (correctif `min-width:0` requis sur l’élément flex imbriqué, sans quoi rien ne le forçait à respecter la largeur disponible — détecté par l’assertion `scrollWidth` déjà présente dans le test de captures, confirmé par capture réelle) ; le mois actif se recentre automatiquement au montage et à chaque redimensionnement.

Relu indépendamment par l’agent `finance-verification` (distinct de l’auteur), en conditions réelles : navigation clavier (Tab, Espace, Entrée, focus visible confirmé par `getComputedStyle`), mise à jour réelle des données dépendantes sur un coffre de démonstration (Décembre vide les opérations datées mais conserve l’occurrence récurrente), frontière décembre↔janvier par clic direct sur les mois (pas seulement les boutons année), recentrage après redimensionnement simulé, rechargement/déverrouillage. A aussi étendu la couverture de débordement à 390 px aux pages non capturées par le test officiel (Mon mois, Mes comptes, Investissements, Documents et réglages, Épargne et projets), un trou de couverture préexistant du test qu’elle a signalé sans le corriger elle-même. Un défaut réel non bloquant trouvé et corrigé : la rangée de mois (`<div aria-label="Choisir un mois">`) n’avait pas de rôle ARIA explicite, donc son nom accessible était silencieusement ignoré (rôle implicite `generic`) ; corrigé par `role="group"`, avec test de régression dédié.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**101/101**, inchangé — aucune fonction de domaine modifiée), `pnpm run build` (réussi), `pnpm audit --audit-level high` (aucune vulnérabilité), les 5 scénarios `test:e2e` rejoués individuellement (dont le nouveau test dédié au sélecteur de mois). Captures 390/834/1440 px inspectées visuellement, y compris la page Épargne et projets.

### Cohorte d’échéances

`occurrenceCohort(data, month)` (`src/domain/finance.ts`) complète le flux réalisé déjà en place (`transactionsForMonth`/`monthSummary`, inchangés) avec l’axe manquant : pour chaque récurrence, l’occurrence due ce mois-ci avec son éventuel règlement lié, quel que soit le mois où ce règlement a réellement eu lieu. Seul un statut `settled` ferme une occurrence ; un lien vers une transaction encore `planned`/`unknown` ne règle rien. La date du règlement peut être nulle : jamais inventée, reflète exactement le champ source. `occurrenceDueDate`, extrait sans changement de comportement du corps de `transactionsForMonth`, est désormais partagé par les deux fonctions plutôt que dupliqué. Aucune page n’appelle encore `occurrenceCohort` : ce lot n’ajoute pas d’interface, c’est le lot V2.3 (page Abonnements) qui en aura besoin.

Relu indépendamment par l’agent `finance-verification` (distinct de l’auteur), en conditions réelles : comparaison bit à bit avec l’ancien code de `transactionsForMonth` (comportement confirmé identique, y compris la validation du mois avec une liste de récurrences vide), 7 cas limites écrits et exécutés indépendamment (récurrence annuelle et trimestrielle réglées plusieurs mois plus tard, deux récurrences partageant la même date d’occurrence sans jamais se confondre, règlement d’un montant différent du dû jamais falsifié, absence de mutation de `data` confirmée par gel `Object.freeze`, mois sans occurrence, mois invalide rejeté). Verdict « prêt à publier tel quel », aucun défaut trouvé.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**109/109**, 9 nouveaux tests dont l’exemple obligatoire d’abonnements.md : une charge de 100 CHF due le 28 février et payée le 2 mars donne 100 CHF dus et 100 CHF réglés en cohorte de février, 0 CHF de flux réalisé en février et 100 CHF en mars, l’échéance de mars restant distincte), `pnpm run build` (réussi). Aucun fichier UI touché ; scénarios e2e concernés rejoués individuellement, inchangés.

## Lot V2.3 — Page Abonnements (développé et testé le 18 septembre 2026)

Septième page (`src/App.tsx`, `page === "subscriptions"`), spécifiée par `.claude/skills/finance/references/abonnements.md`. Résumé compact nommant explicitement ses deux axes : cohorte d’échéances (`Dû en <mois>`, `Réglé pour <mois>`, `Reste dû`, `Récurrences actives`, via `cohortSummary`) et flux réalisé (`Payé en <mois>`, `Reçu en <mois>`, via la nouvelle fonction `recurringFlowSummary`). Liste **Actifs** : identité, nature (Abonnement/Charge/Revenu récurrent/Épargne/À vérifier), cadence, prochain montant et son équivalent mensuel pour une cadence trimestrielle ou annuelle (`monthlyEquivalentMinor`, `nextOccurrenceDate`), compte, statut de l’occurrence du mois sélectionné et action « Marquer payé/reçu » (ou « Remettre à prévu ») réutilisant exactement le même identifiant de transaction virtuelle (`recurrenceId:occurrenceDate`) que Mon mois, donc sans jamais créer de doublon quelle que soit la page utilisée pour régler une occurrence. Liste **En pause ou terminés** repliée par défaut (`<details>`). Filtres Tous/Abonnements/Charges/Revenus et Tous les statuts/Payé-Reçu/À payer-recevoir. Tri Montant mensuel (converti vers la devise d’affichage, comme `rankAccounts`) ou Prochaine échéance. La carte récurrences de Mon mois devient un aperçu en lecture seule (4 récurrences actives maximum, sans ajout ni édition) qui renvoie vers la page dédiée au lieu de dupliquer la gestion.

Relu indépendamment par l’agent `finance-verification` (distinct de l’auteur), en deux passes, avec reproduction indépendante des scénarios (pas seulement relecture du code) :

1. **Première passe** — 3 défauts bloquants réels trouvés :
   - `cohortSummary` et `recurringFlowSummary` ne filtraient pas les récurrences `recurrenceType: "saving"` (mise de côté) : le commentaire de `recurringFlowSummary` prétendait déjà les exclure, mais le code ne filtrait que `kind === "transfer"`, une valeur qu’une `Recurrence` ne porte jamais (seulement `income`/`expense`) — donc une mise de côté réglée gonflait à tort le total dû/réglé/payé. Corrigé : `OccurrenceCohortItem` porte désormais `recurrenceType`, `cohortSummary` exclut `recurrenceType !== "saving"`, `recurringFlowSummary` exclut via une recherche par identifiant de récurrence. Deux tests de régression ajoutés.
   - Le tri « Montant mensuel » comparait des équivalents mensuels dans leur devise propre sans conversion, classant à tort une récurrence en devise étrangère moins chère en apparence qu’une récurrence plus chère une fois convertie. Corrigé : conversion vers la devise d’affichage avant comparaison, avec repli en fin de liste pour un taux manquant — même contrat que `rankAccounts`.
   - Le libellé « Abonnements et charges actifs » promettait un périmètre plus étroit que `activeCount`, qui compte toute récurrence active (revenus et mises de côté compris). Renommé « Récurrences actives ».
   - Aucun test e2e n’exerçait le parcours propre à Abonnements (marquer payé depuis la page elle-même, propagation vers Mon mois et l’Accueil), alors qu’abonnements.md l’exige explicitement (« Le parcours navigateur doit vérifier qu’un statut changé sur Abonnements met à jour Mon mois et l’Accueil après rechargement et déverrouillage, sans doublon »). Ajouté : un scénario dédié couvrant exactement ce chemin, avec rechargement et déverrouillage.
2. **Seconde passe** — les quatre corrections ci-dessus reconfirmées par reproduction indépendante (dont un scénario à 3 devises et taux manquant, au-delà du cas initial à 2 devises), aucun autre défaut trouvé sur ce diff. Un défaut **préexistant, distinct** a été découvert par la même occasion et n’est pas corrigé par ce lot (voir « Prochaine action » ci-dessous) : `monthSummary` (utilisée par Mon mois et l’Accueil, pas par cette page) n’exclut pas non plus `recurrenceType: "saving"`.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**133/133** ; 131/131 avant la correction des deux défauts « mise de côté », 2 tests de régression dédiés ajoutés), `pnpm run build` (réussi). `pnpm run test:e2e` : les 6 scénarios passent rejoués individuellement, dont le nouveau scénario Abonnements rejoué trois fois d’affilée sans échec par la seconde revue. Aucune capture dédiée à ce lot (à faire lors d’un futur lot visuel).

## Lot V2.4 (comptes, répartitions et abonnements) — développé et testé le 18 septembre 2026

Couvre les comptes, les répartitions et désormais les abonnements, les listes citées par `docs/PLAN_AMELIORATION_V2.md` (ligne 36 : « Tri : montant décroissant par défaut pour comptes/abonnements/répartitions »). Le tri des abonnements, qui dépendait du lot V2.3, est câblé dans la page Abonnements (voir section V2.3 ci-dessus) avec conversion multidevise vers la devise d’affichage, corrigée lors de la revue de V2.3.

`accountValue()` est extrait du corps de `wealthSummary` sans changement de comportement (101/101 tests, dont tous ceux de patrimoine déjà existants, passent sans modification), exposant la valeur comparable d’un compte et sa date de valorisation. `rankByValue<T>` est une fonction générique de tri décroissant avec regroupement « À valoriser » pour tout élément sans valeur et date de valorisation communes, clé secondaire stable pour les égalités, qui ne normalise jamais elle-même une mesure (un montant annuel brut n’est pas transformé en équivalent mensuel — c’est à l’appelant de fournir des valeurs déjà comparables). `rankAccounts` l’applique aux comptes, câblé dans l’aperçu Accueil (3 premiers), la page Mes comptes complète et le filtre investissement de la page Investissements — répond au constat de `docs/AUDIT_UI_V2.md` (« `data.accounts.slice(0,3)` suit l’ordre d’insertion »). La légende du donut « Répartition du patrimoine » (Accueil) suit désormais le même ordre plutôt que celui, non trié, de `wealthSummary`.

Relu indépendamment par l’agent `finance-verification` (distinct de l’auteur), en conditions réelles (pas seulement lu dans le code) : changement de devise d’affichage en direct avec et sans taux disponible, ajout d’un compte en devise sans taux (confirmé dans le groupe « À valoriser », jamais de position devinée), plusieurs comptes d’investissement de valeurs différentes. Verdict « prêt à publier tel quel » sur le câblage des comptes, avec une limite trouvée par recherche large dans le code (pas une régression du diff relu) : la répartition du patrimoine n’était pas encore triée — corrigée dans un commit séparé et revérifiée (mêmes contrôles, capture réelle inspectée).

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**101/101**, 8 nouveaux tests sur le tri : ordre décroissant, dette, absence de taux commun, zéro, composantes incomplètes, égalités, non-normalisation annuel/mensuel, élément valorisé mais non daté ; aucune fonction de domaine modifiée par le câblage lui-même), `pnpm run build` (réussi), les 4 scénarios `test:e2e` rejoués individuellement. Captures 390/834/1440 px inspectées visuellement après chaque changement, pas seulement testées automatiquement.

## Correctif — mises de côté dans `monthSummary` (corrigé et testé le 18 septembre 2026)

Défaut préexistant depuis V2.1 (pas une régression de V2.3), découvert par la seconde revue indépendante de la page Abonnements : `monthSummary` (`src/domain/finance.ts`, alimente Mon mois et l’Accueil — Dépenses/Revenus confirmés, Projection nette du mois) ne filtrait que `t.kind === "transfer"` pour exclure les virements, or une `Recurrence` n’a jamais ce `kind` (seulement `income`/`expense`, voir `types.ts`) ; une récurrence classée `recurrenceType: "saving"` réglée gonflait donc à tort ces totaux, alors que `cohortSummary` et `recurringFlowSummary` excluaient déjà correctement `saving` depuis le lot V2.3.

Corrigé en reconnaissant l’exclusion par la classification de la récurrence liée (comme `cohortSummary`/`recurringFlowSummary`) : une transaction ou occurrence virtuelle liée à une récurrence `saving` est désormais hors périmètre, ni comptée dans les totaux ni dans `unknownCount` (elle n’est pas « inconnue », juste hors sujet, comme un virement). Deux tests de régression ajoutés, confirmés par contrôle négatif (échouent sans le correctif, rejoué manuellement avec les valeurs fautives attendues).

Relu indépendamment par l’agent `finance-verification` (distinct de l’auteur), avec reproduction indépendante plutôt que relecture seule : invariant de type confirmé par lecture croisée de `validation.ts`/`migration.ts` (une récurrence `saving` ne peut structurellement être que `kind: "expense"`, appliqué sur tous les chemins d’entrée de données, pas seulement par convention) ; contrôle négatif rejoué indépendamment (code d’avant le correctif restauré temporairement, 2 échecs confirmés avec les mêmes valeurs fautives) ; scénario navigateur réel écrit indépendamment (compte, mise de côté réglée de 200 CHF, vraie dépense de 150 CHF, vrai revenu de 1000 CHF) montrant Dépenses payées/confirmées et Projection nette correctes avec le correctif (150.00 CHF, pas 350.00 CHF) contre fautives sans lui (350.00 CHF), y compris après rechargement et déverrouillage. Verdict « peut être poussé », aucun défaut trouvé.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135** ; 133/133 avant ce correctif, 2 tests de régression dédiés ajoutés), `pnpm run build` (réussi), les 6 scénarios `test:e2e` rejoués individuellement (aucun fichier UI ni test e2e modifié par ce correctif). Le sujet des mises de côté (« ne gonflent pas dépenses et revenus ») est désormais honoré de bout en bout dans `cohortSummary`, `recurringFlowSummary` et `monthSummary`.

## Lot V2.5 — Cartes de comptes compactes (développé et testé le 18 septembre 2026)

Critère du plan (`docs/PLAN_AMELIORATION_V2.md`) : « Cinq comptes usuels visibles à 1 000 px ; cibles tactiles 44 px ; aucun débordement. » `docs/AUDIT_UI_V2.md` avait mesuré la carte de compte à ~323px de hauteur (padding 24px, rayon 22px), avec seulement 3 comptes visibles sur 6 à 1440×1000.

`src/index.css` uniquement : `.account-card` (padding 24→18px, rayon 22→20px, aligné avec `identite-ui.md` : « environ 18–20 px pour les grandes cartes »), `.account-head` (marge basse 31→14px), `.balance` (marge basse 8→6px), nouvelles règles scoping `.account-card .meta`/`.account-card .hero-foot` (au lieu de toucher ces classes globalement, réutilisées ailleurs dans l’app), `.account-history` (partagée avec la liste « En pause ou terminés » de la page Abonnements ; marge/padding resserrés). Point de rupture téléphone (`≤600px`) resserré proportionnellement plutôt que laissé tel quel (padding 22→16px, marge d’en-tête 25→12px). Les cibles tactiles (bouton Actualiser, bascule Historique, bouton Modifier) gardent leur `min-height`/`min-width: 44px` explicite, indépendant des paddings retouchés.

Mesuré réellement dans le navigateur (script Playwright ad hoc, supprimé après usage, jamais deviné) : hauteur de carte 323→271px à 1440×1000, 6 comptes fictifs désormais tous visibles sans défiler (contre 3 avant) ; à 390px, 300→275px, toujours sans débordement avec un nom de compte et d’établissement longs (plusieurs mots).

Relu indépendamment par l’agent `finance-verification` (distinct de l’auteur), avec reproduction indépendante (script Playwright propre à la revue, comptes créés via le vrai formulaire, pas d’injection) : confirme 323→271px et 3/6→6/6 comptes visibles sur son propre jeu de 6 comptes ; confirme 5/5 comptes usuels visibles sans débordement (le critère exact du plan) ; cibles tactiles vérifiées ≥44px y compris montants masqués et compte sans solde ; scoping `.account-card .meta`/`.account-card .hero-foot` confirmé sans effet de bord sur le tableau de bord ou les cartes de projets ; espacement de `.account-history` sur la page Abonnements confirmé correct. **Défaut réel trouvé, non bloquant pour ce lot** : un nom de compte ou d’établissement composé d’un seul mot très long sans espace déborde horizontalement (`.institution`/`.account-head h3` n’ont pas de `overflow-wrap`, contrairement à `.balance`) — confirmé **préexistant** en rejouant le même scénario sur le commit parent, donc ni introduit ni aggravé par ce lot ; reste à corriger séparément (voir « Prochaine action »).

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé — ce lot est uniquement CSS), `pnpm run build` (réussi), les 6 scénarios `test:e2e` rejoués individuellement par l’auteur et par le relecteur. Captures 390/834/1440 px régénérées par le test e2e existant et conservées (montrent un changement visuel réel et voulu) ; inspectées visuellement par l’auteur et le relecteur, uniquement la démonstration fictive.

## Correctif — débordement sur nom de compte/établissement sans espace (corrigé et testé le 18 septembre 2026)

Défaut préexistant trouvé par la revue de V2.5 (voir ci-dessus), corrigé séparément : `.institution` et le `<h3>` du nom de compte n’avaient pas `overflow-wrap`, contrairement à `.balance`. Un seul mot très long sans espace (nom d’établissement importé) débordait horizontalement dès que la grille de comptes n’avait plus de cellule vide pour absorber le débordement (5-6 comptes remplissant les 3 colonnes à 1440×1000).

Corrigé en ajoutant `overflow-wrap: anywhere` sur la règle générique `h1`/`h2`/`h3` (couvre aussi le nom de projet et le titre d’un élément à vérifier, mêmes risques) et sur `.institution`. Test e2e dédié ajouté, confirmé par contrôle négatif (échoue sans le correctif, rejoué manuellement avec le même scénario que la revue : six comptes, un nom d’établissement à mot unique très long).

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé — correctif CSS), `pnpm run build` (réussi), les 7 scénarios `test:e2e` rejoués individuellement.

## Lot V2.6 — Icônes et établissements (développé et testé le 18 septembre 2026)

Répond aux deux constats de l’audit initial (« un petit vocabulaire d’icônes », « identités peu distinctes ») en étendant le composant SVG interne existant (grille 24×24) plutôt qu’en ajoutant une dépendance externe : le besoin restait une seule icône manquante. Nouvelle icône « vault » (coffre à dial) pour les récurrences classées épargne/mise de côté — aucune icône existante ne convenait sans collision de métaphore (« target » désigne déjà les projets/objectifs).

Chaque nature de récurrence a désormais sa propre icône dans la liste Abonnements et l’aperçu de Mon mois, au lieu du même pictogramme de répétition pour toutes : abonnement → refresh, charge → bank (déjà présente, inutilisée jusqu’ici), revenu récurrent → arrow-down (même icône que les revenus ailleurs dans l’app), épargne → vault (nouvelle), à vérifier → alert. Monogramme d’établissement (comptes) et de titre (positions) avec initiales mot par mot (« Banque Fictive » → BF au lieu de BA) et couleur stable dérivée du nom, dans une palette limitée à la famille bleu-violet de l’app — reste local et déterministe, aucun logo réel téléchargé ni registre de marques.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé), `pnpm run build` (réussi).

## Lot V2.7 — Vérification du logo Finance existant (vérifié le 18 septembre 2026)

`public/finance.svg` (et ses déclinaisons `finance-maskable.svg`, `icon-192.png`, `icon-512.png`, `icon-maskable-512.png`) existaient déjà et couvrent le critère du plan « signe original cohérent du favicon à l’écran d’accueil » sans qu’un nouveau signe soit nécessaire : vérifié à 16/24/48/128px sur fond clair et sombre, aucune perte de lisibilité. Cette vérification n’est donc pas un nouveau dessin, mais un contrôle du logo existant contre les critères V2.7.

Le contrôle a trouvé deux défauts réels, corrigés dans le même commit que ceux listés ci-dessous : `index.html` liait `apple-touch-icon` vers `icon-192.png` (fond transparent, que iOS affiche en noir faute d’opacité) au lieu du PNG dédié 180×180 à fond opaque déjà présent dans `public/` mais jamais câblé ; et la 3ᵉ couleur de `MONOGRAM_PALETTE` (V2.6, cyan `#7ac4de`) sortait de la famille bleu-violet du reste de la palette, contredisant le principe énoncé dans le lot V2.6 lui-même — remplacée par un bleu de la même famille.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**), `pnpm run build` (réussi), inspection visuelle du PNG corrigé (180×180, RGB opaque, sans transparence).

## Correctif — débordement sur `.review-item p` (texte libre importé) (corrigé et testé le 18 septembre 2026)

Même classe de bug que le correctif compte/établissement ci-dessus, trouvée par une revue indépendante distincte sur un autre élément : `.review-item p` (carte « Informations à vérifier », Documents et réglages) n’avait pas `overflow-wrap`, contrairement à `.review-item h3` (couvert par la règle générique `h1`/`h2`/`h3` du premier correctif). Le champ `reason` d’un élément à vérifier est du texte libre importé (jusqu’à 8000 caractères, `src/domain/validation.ts`) pouvant contenir un mot unique sans espace.

Corrigé par `overflow-wrap: anywhere` sur `.review-item p`. Test e2e dédié ajouté et vérifié par double contrôle négatif : la première version du test (mot de 79 caractères) ne débordait pas la page à 1440px car cette carte est pleine largeur sur sa propre ligne (contrairement à la grille à 3 colonnes des comptes) — corrigé en répétant le mot ×4 (~316 caractères), confirmé par reconstruction (`git stash` du correctif CSS + `npm run build`, car `test:e2e` sert `dist/` via `vite preview` et non les sources en direct) : le test échoue sans le correctif, passe avec.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé), `pnpm run build` (réussi), les 8 scénarios `test:e2e` rejoués individuellement (le bac à sable Chromium single-process reste intermittent en exécution groupée, limite déjà documentée, pas une régression).

## Lot V2.8 — Livraison finale (vérifiée et publiée le 18 septembre 2026)

Critère du plan : « Typecheck, tests, build, E2E, CI, Pages et reprise Claude documentés. » Passe de vérification finale sur la PR #5 (titre et description mis à jour pour refléter les sept lots livrés V2.1–V2.7 au lieu des cinq initiaux) :

- `pnpm run typecheck` : 0 erreur.
- `pnpm run test` : **135/135**, 6 fichiers.
- `pnpm run build` : réussi (`tsc -b && vite build && node scripts/build-offline.mjs`).
- `pnpm audit --audit-level high` : aucune vulnérabilité.
- `pnpm run test:e2e` : les **8** scénarios (et non 9, erreur de comptage corrigée dans la section précédente) rejoués individuellement, tous verts.
- CI (`Finance verification`, workflow `verify.yml`) : succès sur les deux derniers commits de la PR #5 (`c8a7174`, `da0745c`).

**Fusionnée dans `main`** (commit de fusion `12f1d1d`) et **déployée** : CI et le workflow GitHub Pages (`pages.yml`) tous deux verts sur ce commit, site public à jour sur `https://mendestrading21.github.io/Finances/`. Reprise Claude : `DEMARRER_CLAUDE.md` reste valable tel quel (il demande explicitement de revérifier l'état courant plutôt que de faire confiance aux SHA ou totaux historiques) ; aucune modification nécessaire.

## Correctif — mise à jour du service worker PWA (corrigé et testé le 18 septembre 2026)

Signalé par l'utilisateur après la fusion de V2.8 : la page Abonnements (livrée dans V2.3, déjà dans le code et la navigation) restait invisible dans son navigateur malgré un déploiement réussi. Cause réelle, sans rapport avec la page elle-même : `scripts/build-offline.mjs` génère `dist/sw.js`, dont les gestionnaires `install`/`activate` n'appelaient ni `self.skipWaiting()` ni `self.clients.claim()`. Sans eux, un nouveau déploiement reste « en attente » indéfiniment et un onglet déjà ouvert (ou l'app installée) continue de servir l'ancien shell tant qu'il n'est pas entièrement fermé puis rouvert — un simple rechargement ne suffit pas. Un utilisateur ayant l'app ouverte avant un lot pouvait donc ne jamais voir ce lot, sans indice sur la raison.

Corrigé par `self.skipWaiting()` à l'installation et `self.clients.claim()` à l'activation. `src/main.tsx` distingue la toute première prise de contrôle (qui suit un chargement normal, donc déjà à jour — ne rien faire) d'un changement de contrôleur alors qu'un contrôleur existait déjà (vraie mise à jour pendant que l'onglet reste ouvert — notifier). Trouvé en testant : une première version sans cette distinction rechargeait aussi à la toute première visite, cassant le test e2e « private vault » (rechargement interrompant un clic de navigation) ; corrigé avant publication.

Relu indépendamment par l'agent `finance-verification` (distinct de l'auteur), qui a reproduit le contrôle négatif lui-même (fichier d'avant le correctif restauré, rebuild, test rejoué, échec confirmé) et trouvé deux défauts réels supplémentaires, corrigés avant publication :

1. **Rechargement silencieux.** Le correctif initial rechargeait l'onglet directement dès qu'une mise à jour était détectée. Or le coffre ne vit qu'en mémoire (`vault.ts` : seule l'enveloppe chiffrée est persistée), donc ce rechargement reverrouillait systématiquement le coffre et perdait toute saisie en cours, sans avertissement — un utilisateur en plein milieu d'une saisie se retrouvait soudainement déconnecté sans explication. Corrigé : `main.tsx` notifie désormais un événement (`finance:update-ready`, `src/swUpdateEvent.ts`, partagé entre les deux fichiers pour éviter toute dérive du nom) au lieu de recharger directement ; `App.tsx` affiche un bandeau persistant (« Une nouvelle version de Finance est disponible. Recharger ») avec un bouton explicite, uniquement dans la vue déverrouillée — seul endroit où reverrouiller un coffre ouvert est un risque réel.
2. **Risque de collision dans le test e2e.** Le test mutait `dist/sw.js` et `dist/index.html` directement sur disque, partagés avec le serveur `vite preview` utilisé par tous les autres tests — or `playwright.config.ts` a `fullyParallel: true` et 2 workers par défaut (ce que `pnpm run test:e2e` utilise réellement en CI), donc un autre test exécuté en parallèle aurait pu lire ces fichiers pendant la mutation. Corrigé : le test copie désormais `dist/` dans un répertoire temporaire privé et le sert via son propre petit serveur HTTP (`node:http`), isolé de tout autre test quel que soit le nombre de workers.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé), `pnpm run build` (réussi), les 9 scénarios `test:e2e` rejoués individuellement, dont le nouveau test dédié avec double contrôle négatif (échoue proprement à ~15s sans `self.skipWaiting()`/`self.clients.claim()`, rejoué manuellement par l'auteur et par le relecteur). Un essai en parallélisme réel (`--workers=2`, configuration par défaut, comme la CI) a été tenté : les échecs observés dans ce bac à sable sont des plantages génériques de Chromium single-process déjà documentés ailleurs dans ce fichier (erreurs dbus/SSL, « browser has been closed »), jamais une collision de contenu — aucune assertion du test PWA n'a échoué sur un contenu inattendu, dans aucun des essais.

À la même occasion : le sous-titre de la page Abonnements (`src/App.tsx`) mentionne désormais « factures » et « charges récurrentes » explicitement. La fonctionnalité (récurrence Nature « Charge », filtre dédié sur la page) couvrait déjà le besoin décrit par l'utilisateur (factures mensuelles toujours identiques + extras occasionnels via Mon mois) ; seule la découvrabilité manquait — confirmé en relisant `Editor.tsx` (option « Charge (loyer, assurance…) ») et le filtre de la page Abonnements, pas seulement supposé.

## Correctif — regroupement des actions de ligne sur mobile (corrigé et testé le 18 septembre 2026)

Signalé par l'utilisateur avec trois captures de l'app réelle sur son iPhone (390 px) : le bouton « Marquer payé » (carte « Prochaines échéances » de l'Accueil, et liste « Actifs » d'Abonnements) semblait désorganisé, flottant sans lien visuel clair avec le reste de la ligne. Reproduit exactement (même scénario : un compte, une récurrence « Assurance santé » à CHF 420.00) et mesuré : l'alignement était techniquement correct (montant et bouton tous deux alignés à droite), mais `.row-value` et le(s) bouton(s) d'action étaient chacun un enfant flex indépendant de `.row` — à 390 px, aucun des deux ne tient à côté du titre, donc chacun passe sur sa propre ligne avec le même espacement de 12px que le reste de la ligne, ce qui fait lire la ligne comme trois blocs empilés sans rapport plutôt que comme une seule ligne cohérente.

Corrigé (`src/App.tsx` : `transactionRow`, utilisé par Mon mois et la carte « Prochaines échéances » de l'Accueil ; `subscriptionRow`, utilisé par la liste « Actifs » d'Abonnements) en regroupant `.row-value` et les boutons d'action dans un conteneur commun `.row-end` (`src/index.css`), avec un espacement interne plus resserré que celui qui le sépare de l'en-tête de la ligne. Sur l'Accueil (pas de deuxième ligne « Prochaine échéance » sous le montant), le montant et « Marquer payé » tiennent désormais sur une seule ligne à 390 px. Sur Abonnements (montant + date d'échéance sur deux lignes), le montant et le bouton restent sur des lignes séparées faute de place, mais forment maintenant un bloc visuellement resserré au lieu de deux lignes flottantes. Aucun texte, libellé, statut ni calcul modifié — restructuration DOM/CSS pure. Les listes Investissements et Documents, qui gardent `.row-value`/`.button`/`.icon-button` en enfants directs de `.row`, ne sont pas touchées.

Développé par l'agent `finance-designer` (voir `.claude/agents/finance-designer.md`), qui n'a pas pu obtenir de relecture indépendante dans son propre environnement (aucun outil d'agent disponible dans le sien) — signalé explicitement plutôt que revendiqué à tort. Relu ensuite, séparément, par l'agent `finance-verification` (distinct de l'auteur), qui a reproduit indépendamment (coffre et données propres, pas de réutilisation de l'état du designer) et trouvé un défaut réel : en sortant les boutons icône de leur ancien parent direct `.row`, ils sortaient aussi de la portée du sélecteur `.row > .icon-button` (fond et bordure transparents), gagnant silencieusement un contour/fond visible qu'ils n'avaient pas avant — confirmé par styles calculés (`getComputedStyle`) comparés au cas resté inchangé (liste Investissements). Corrigé par une règle `.row-actions .icon-button` dédiée, reconfirmé par styles calculés identiques au cas non touché. Ce correctif ayant été écrit par le relecteur lui-même, une seconde vérification indépendante restait nécessaire avant publication (règle du skill) : faite par le coordinateur (build, styles calculés recontrôlés, 9/9 scénarios e2e rejoués individuellement).

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé — changement CSS/JSX pur), `pnpm run build` (réussi), les 9 scénarios `test:e2e` rejoués individuellement par l'auteur, le relecteur et le coordinateur. Aucun débordement à 390/834/1440 (`scrollWidth === clientWidth` vérifié par JS, pas seulement visuellement). Cibles tactiles inchangées (44×44). Focus clavier réel (Tab, pas `.focus()`) confirmé non tronqué par les nouveaux conteneurs. Captures avant/après à 390/834/1440 dans `docs/captures/` (mêmes captures que V2.8, régénérées par le test e2e officiel) et captures ad hoc (non versionnées) montrant le rapprochement visuel réel sur l'Accueil et Abonnements.

Fusionné dans `main` (commit `de04fb4`, PR #7). CI et GitHub Pages vérifiés verts sur ce commit, site public à jour.

## Ajout — carte « Le mouvement du mois » sur l'Accueil (développé et testé le 21 septembre 2026)

Demandé par l'utilisateur, qui a montré son tableau de bord Notion personnel « Patrimoine » (table de comptes avec donut de répartition, graphique en barres entrées/sorties, panneaux Revenus/Épargne/Factures/Abonnements) en demandant « quelque chose de similaire mais plus simple, mêmes visuels, mieux adapté ». L'Accueil (`page === "overview"`) avait déjà l'équivalent du patrimoine + sa courbe, du donut de répartition et des prochaines échéances, mais pas le pendant visuel en barres des entrées/sorties du mois — seul un tableau de chiffres (« Votre mois ») l'exprimait. Un composant `FlowChart` (barres de progression Entrées/Dépenses, `src/components/Charts.tsx`) existait déjà et était câblé sur la page « Mon mois », mais jamais sur l'Accueil.

La section qui affichait « Répartition du patrimoine » et « Prochaines échéances » en deux colonnes (`.two-columns`) passe à trois colonnes (`.three-columns`, classe déjà existante et déjà responsive — repasse à une colonne sur tablette et mobile), avec une nouvelle carte « Le mouvement du mois » insérée entre les deux, réutilisant exactement le même calcul `income`/`expense` que celui déjà câblé sur « Mon mois » (`summary.incomePlanned + summary.incomeSettled`, `summary.expensePlanned + summary.expenseSettled`, propagation de `null` inchangée) — aucune nouvelle fonction de domaine, aucun nouveau champ, un seul branchement supplémentaire d'un composant et d'une donnée déjà testés et déjà affichés ailleurs sur la même page.

Relu indépendamment par l'agent `finance-verification` (distinct de l'auteur), en conditions réelles : parité exacte des montants entre les cartes « Le mouvement du mois » de l'Accueil et de « Mon mois » confirmée par lecture du DOM des deux pages et recalcul indépendant depuis le `stat-grid` de Mon mois (aucune divergence) ; absence de débordement mesurée (`scrollWidth` vs `innerWidth`) à 390/834/1440 px, avec la grille confirmée à 3 colonnes sur ordinateur et 1 colonne sur tablette/mobile (`getComputedStyle`) ; mode « Montants masqués » testé par clic réel (aucun montant résiduel dans la nouvelle carte) ; captures régénérées inspectées visuellement, aucune régression sur les autres cartes de l'Accueil ni sur Mon mois. Verdict « conforme, aucun défaut trouvé ».

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé — branchement UI pur, aucune fonction de domaine modifiée), `pnpm run build` (réussi). Les 9 scénarios `test:e2e` rejoués individuellement par l'auteur et le relecteur (un essai en exécution groupée a montré les plantages Chromium single-process déjà documentés dans ce fichier — dbus/SSL, « browser has been closed » — reconfirmés comme limite d'environnement, pas une régression : les mêmes scénarios passent tous rejoués un par un). Captures 390/834/1440 régénérées et conservées (changement visuel réel) ; les captures des pages non concernées (coffre, projets) ont été écartées du diff car elles ne reflétaient qu'un nouveau rendu du même contenu.

Fusionné dans `main` (commit `525ee77`, PR #8). CI et GitHub Pages vérifiés verts sur ce commit, site public à jour.

## Ajout — section « Aperçu du mois » sur l'Accueil (développé et testé le 21 septembre 2026)

Deuxième volet de la demande de l'utilisateur (tableau de bord Notion personnel « Patrimoine », dont un bloc multi-panneaux « Revenue / Epargne 2026 / Facture 2026 / Abonnements »). Nouvelle section entre « Votre mois » et « Vos comptes » sur l'Accueil : quatre `stat-card` dans un `.stat-grid` (classe déjà existante, déjà responsive — 4 colonnes sur ordinateur, 2 sur tablette, 1 sur mobile, utilisée ailleurs par Mon mois et Abonnements) — Revenus (reçus et attendus, calcul identique à la carte « Votre mois »), Abonnements (mensuel), Factures et charges (mensuel), Épargne (mensuel).

Les trois derniers utilisent une nouvelle fonction locale `monthlyEquivalentTotal(type)` (`src/App.tsx`) : somme, pour les récurrences actives d'une nature donnée, de `monthlyEquivalentMinor(r, today())` convertie vers la devise d'affichage (`convertMinor`) — le même schéma déjà utilisé et testé par le tri « Montant mensuel » de la page Abonnements, pas une nouvelle fonction de domaine. Volontairement pas la cohorte du mois (`cohortSummary`) : une récurrence trimestrielle ou annuelle afficherait 0 la plupart des mois si on utilisait le montant dû ce mois-ci au lieu de l'équivalent mensuel normalisé, qui existe déjà précisément pour ce cas. Une conversion manquante fait passer le total entier à « — » plutôt que de mélanger des montants non comparables ou de les ignorer silencieusement ; le nombre de récurrences actives de cette nature reste affiché séparément.

Relu indépendamment par l'agent `finance-verification` (distinct de l'auteur), avec reproduction indépendante plutôt que lecture seule : scénario construit dans l'app réelle avec plusieurs récurrences actives de natures et devises différentes (abonnement trimestriel EUR, charge annuelle CHF, charge mensuelle USD, épargne CHF, épargne GBP sans taux configuré, récurrence inactive), calcul manuel indépendant confirmant chaque total affiché au centime près ; récurrence inactive jamais comptée ; taux manquant confirmé faire passer le total à « — » (pas 0, pas silencieusement ignoré) ; non-régression du tri « Montant mensuel » de la page Abonnements confirmée sur le même jeu de données ; absence de débordement à 390/834/1440 ; mode « Montants masqués » testé par clic réel. Verdict « conforme, aucun défaut trouvé ».

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé — aucune fonction de domaine ajoutée, la nouvelle fonction reste locale à `App.tsx`), `pnpm run build` (réussi). Scénarios `test:e2e` touchant l'Accueil et Abonnements rejoués individuellement par l'auteur et le relecteur, tous verts.

Fusionné dans `main` (commit `e51f39a`, PR #9). CI et GitHub Pages vérifiés verts sur ce commit, site public à jour.

## Ajout — badges de nature de compte (développé et testé le 21 septembre 2026)

Troisième volet de la demande de l'utilisateur de rapprocher visuellement Finance de son tableau de bord Notion personnel (table de comptes avec badges colorés de nature, façon propriété « select »). La nature d'un compte (`Account.kind` : bancaire/épargne/investissement/dette) n'était affichée qu'en texte discret dans `.meta` (« Solde au JJJJ-MM-JJ · Investissement »). `accountCard()` (`src/App.tsx`, réutilisée telle quelle sur Accueil, Mes comptes et Investissements) affiche désormais un badge coloré (« pill ») à côté du nom d'établissement, avec le même libellé que le dictionnaire `kinds` déjà existant — aucune donnée inventée. Le suffixe redondant a été retiré de `.meta`, l'information n'est plus dupliquée, seulement déplacée. Couleur fixe par nature (pas un hash par nom comme les monogrammes), réutilisant 4 teintes déjà existantes de `MONOGRAM_PALETTE` (famille bleu-violet déjà corrigée en V2.7), aucune nouvelle couleur introduite. `.institution` passe de `display:flex` à `display:block` pour que le badge retombe proprement à la ligne avec un nom d'établissement long.

Développé par l'agent `finance-designer`, qui n'a pas commité (consigne explicite de ce lot, pour éviter la mauvaise branche déjà rencontrée une fois dans cette session) — le coordinateur a repris la main pour la relecture, la publication et la fusion. Relu indépendamment par l'agent `finance-verification` (distinct de l'auteur), avec reproduction réelle : non-régression confirmée sur le scénario existant de nom d'établissement long sans espace ; cohérence du badge vérifiée sur les 3 pages qui réutilisent `accountCard` (Accueil, Mes comptes, Investissements) et aux 3 formats ; absence de texte orphelin après la suppression du suffixe dans `.meta`. **Un défaut réel trouvé et corrigé par le relecteur lui-même** : le contraste WCAG calculé par l'auteur (couleur « Dette », `#7c8cff`) ne composait le badge que sur le fond plat de la carte ; mesuré sur les pixels réellement rendus (incluant le dégradé radial du coin de la carte, `.account-card`), ce badge tombait sous le seuil AA (≈4.3–4.5:1) avec un nom d'établissement ordinaire placé près de ce dégradé — un vrai défaut de lisibilité, pas un cas construit. Corrigé en remplaçant cette teinte par `MONOGRAM_PALETTE[5]` (`#c0ceef`, toujours une teinte déjà existante de la même famille, pas une couleur inventée), remesuré par le relecteur à ≈7.6–8.4:1 sur plusieurs positions réelles.

Ce correctif ayant été écrit par le relecteur lui-même, une seconde vérification indépendante restait nécessaire avant publication (règle du skill, déjà appliquée une fois cette session pour le regroupement des actions de ligne) : faite par le coordinateur — confirmation que `MONOGRAM_PALETTE[5]` est bien une teinte déjà existante de la palette (pas inventée), recalcul indépendant du contraste (alpha-blend + luminance WCAG) donnant 8.37:1, cohérent avec la mesure du relecteur, `pnpm run typecheck`/`test`/`build` rejoués, captures régénérées et inspectées visuellement (390/1440, badges Investissement/Épargne/Compte bancaire lisibles, aucun débordement).

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé — changement CSS/JSX pur), `pnpm run build` (réussi), les scénarios `test:e2e` concernant comptes/investissements rejoués individuellement par l'auteur et le relecteur, tous verts. Aucun compte « Dette » dans la démonstration fictive (`src/demo.ts`), donc ce cas précis n'apparaît pas dans les captures officielles régénérées ; vérifié à la place par mesure de pixels réels sur des comptes créés ad hoc pendant la revue (supprimés après usage, hors dépôt).

Fusionné dans `main` (commit `45c59c8`, PR #10). CI et GitHub Pages vérifiés verts sur ce commit, site public à jour.

## Ajout — page « Mes comptes » en vraie table façon Notion (développé et testé le 21 septembre 2026)

Quatrième volet du rapprochement visuel : questionné explicitement, l'utilisateur a choisi une vraie table façon Notion plutôt que garder les cartes ou un mélange responsive carte/table, puis a aussitôt précisé « mieux organisé, plus simple à utiliser, vraiment avoir un suivi de mes finances » — priorité donnée à la lisibilité sur la densité en cas de tension. La page « Mes comptes » (`page === "accounts"`, `src/App.tsx`) passe d'une grille de cartes (`account-grid`/`accountCard`) à une vraie `<table>` sémantique (nouvelle fonction `accountRow`) : colonnes Compte (monogramme + nom + établissement), Type (badge coloré déjà livré), Solde (aligné à droite), Daté (repliée dans la cellule Compte sous 600px plutôt que de forcer un débordement), Actions (Actualiser/Modifier/historique, icônes seules ≥44×44px). `accountCard()` elle-même n'a pas été modifiée et reste utilisée telle quelle sur l'Accueil et Investissements — seule la page Mes comptes devient une table, les aperçus/glances restent des cartes.

Ligne de pied « Total » : réutilise exactement `wealth.totalMinor`/`wealth.partial`, déjà calculé pour la carte « Patrimoine observé » de l'Accueil — aucun second calcul de total introduit. L'historique des soldes datés (`<details>` de l'ancienne carte) devient une ligne dépliable (`aria-expanded`/`aria-controls`/`hidden` natif) sous chaque ligne de compte plutôt que de disparaître.

Développé par l'agent `finance-designer` (sans commit, consigne déjà appliquée deux fois cette session). Sans sous-agent disponible dans son propre environnement pour la relecture, il a lui-même trouvé et corrigé trois défauts réels de rendu en cours de route, tous confirmés par capture/mesure réelle et non par supposition : une colonne Compte quasi écrasée en `table-layout: auto` (colonnes non-wrappables prenant toute la largeur en premier), un montant financier coupé en fragments illisibles par un `overflow-wrap: anywhere` hérité d'un autre contexte, et une ligne Total décalée d'une colonne par un `colSpan` combiné aux colonnes repliées sous 600px.

Relu indépendamment par l'agent `finance-verification` (distinct de l'auteur), en conditions réelles, sur les trois points que l'auteur avait explicitement signalés ne pas avoir pu vérifier lui-même : accessibilité du chevron d'historique confirmée en DOM réel (`aria-expanded`/`aria-controls`/`hidden` cohérents, cycle replié/déplié/replié, navigation clavier) ; contraste des badges repliés à 390px mesuré réellement (5.51–8.37:1, tous ≥ AA) ; cibles tactiles mesurées à 44×44px exactement. Total vérifié identique à `wealth.totalMinor` dans 3 scénarios (démo par défaut, devise sans taux commun → « Partiel », compte sans taux ajouté) en comparant en direct avec la carte « Patrimoine observé » de l'Accueil. Mode « Montants masqués » confirmé sur les soldes de ligne, le total et l'historique déplié. Contenu de l'historique déplié comparé ligne à ligne avec l'ancien `<details>` d'`accountCard` sur le même compte (Investissements) : identique, seule la provenance (`SourceLink`) se retrouve maintenant à l'intérieur du panneau repliable au lieu d'être toujours visible — conforme à `docs/PLAN_AMELIORATION_V2.md` (« l'historique, la provenance détaillée… se déplient »), pas une régression. Une observation du coordinateur (texte du total visuellement plus pâle sur une capture à 390px) n'a pas été reproduite par la mesure (couleur et ratio de contraste strictement identiques à une ligne normale, 16.43:1) : artefact de capture, pas un défaut réel.

**Deux limites signalées explicitement, non bloquantes** : la passe tactile n'a été mesurée qu'en Chromium émulé (bounding box), jamais sur un appareil physique — limite déjà connue et déjà documentée pour l'ensemble de l'app (voir « Appareils physiques : non testé » dans la table ci-dessous), pas propre à ce lot. Le run e2e groupé reste intermittent dans ce bac à sable (erreurs réseau/dbus de Chromium single-process) ; les mêmes scénarios passent tous rejoués individuellement, limite déjà documentée ailleurs dans ce fichier.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé — aucune fonction de domaine touchée), `pnpm run build` (réussi), les 3 scénarios `test:e2e` concernant les comptes rejoués individuellement par l'auteur, le relecteur et le coordinateur, tous verts. Aucun débordement horizontal à 390×844/834×1112/1440×1000 (`scrollWidth <= innerWidth`), vérifié indépendamment par les trois. Aucune capture officielle modifiée (la page Mes comptes n'est pas couverte par le test de captures existant) ; rendu inspecté visuellement par script Playwright ad hoc (supprimé après usage, hors dépôt) aux 3 formats par le coordinateur et le relecteur séparément.

Fusionné dans `main` (commit `696018d`, PR #11). CI et GitHub Pages vérifiés verts sur ce commit, site public à jour.

## Retour utilisateur sur la table et refonte visuelle générale (développé et testé le 22 septembre 2026)

La table livrée ci-dessus a été publiée et vue en ligne par l'utilisateur, qui a réagi négativement (« je suis pas du tout fan fait un autre truc plus beau »). Interrogé pour préciser, il a désigné à la fois la table elle-même et le style général comme insuffisants, et a explicitement laissé carte blanche plutôt que de fournir une référence précise. Ce lot regroupe trois passes successives développées en réponse, chacune montrée à l'utilisateur en capture avant publication (pas seulement décidée en interne) :

1. **Retour aux cartes + finition générale** (agent `finance-designer`) : « Mes comptes » repasse d'une `<table>` à une grille de cartes (`accountCard` fusionnée en une seule fonction, réutilisée sur Accueil/Mes comptes/Investissements comme avant la table), avec un vrai correctif de hiérarchie trouvé sur le rendu réel (le nom du compte, pas l'établissement, doit être le titre primaire — `identite-ui.md`) plutôt qu'une simple restauration de l'ancienne carte. Ombres/profondeur ajoutées à `.card`/`.hero-card`/`.account-card`/`.stat-card`, icônes posées sur les cartes principales via une nouvelle prop `icon` de `Card`.
2. **Icônes, cartes et liste Abonnements affinées** (agent `finance-identite-visuelle`, après un deuxième retour « bof » de l'utilisateur ciblant précisément icônes/cartes/liste des factures) : 3 défauts réels d'alignement optique corrigés dans `src/components/Icon.tsx` (`close`/`plus` de poids différent, `upload`/`download` de poids différent, `more` reposant sur une coïncidence de réglages) ; `SourceLink` déplacé dans le volet d'historique dépliable de la carte de compte (ne restait plus seul en pied de carte) ; les récurrences Abonnements colorées par nature (mêmes teintes que les badges de compte) au lieu d'un fond gris uniforme ; icônes retirées des 14 tuiles `stat-grid` (Aperçu du mois, Mon mois, Abonnements) — `identite-ui.md` limite les icônes aux cartes principales et lignes de comptes/abonnements, pas aux petites tuiles répétées, et deux tuiles adjacentes de sens différent partageaient la même icône.
3. **Icônes de compte et lignes de paiement** (coordinateur, en réponse à « change les icônes des comptes au lieu de laisser les icônes » puis « les paiements du mois plus jolie en liste ») : le monogramme d'établissement (initiales) sur `accountCard` est remplacé par une icône de **nature** de compte (banque/épargne/investissement/dette), une nouvelle icône `debt` ajoutée (aucune icône existante ne correspondait, même schéma que `chart` mais tendance inversée) ; les lignes de dépense sur Mon mois et Prochaines échéances utilisent désormais `.row-icon.negative` (déjà défini en CSS mais jamais appliqué aux dépenses jusqu'ici — chaîne vide à la place) ; `.row` (partagée par toutes les listes de l'app) gagne un `border-radius` et un état `:hover`/`:focus-within` en réponse à la demande explicite « comme sur Notion, simple à utiliser ».

Relu indépendamment par l'agent `finance-verification` (distinct des auteurs), en conditions réelles, sur le dernier état cumulé du diff : icônes de nature de compte vérifiées exactes sur les 3 pages (7/7, aucun mélange) et compte « Dette » créé via le vrai formulaire pour confirmer l'icône neuve lisible à 20px ; contraste WCAG mesuré sur pixels réellement composés pour les 4 variantes de couleur (4.91–8.36:1, toutes ≥ AA) ; lignes de dépense confirmées recolorées (contraste ≈7.6:1) sans changement sur revenus/virements ; hover réel (`page.hover()`) exercé sur les 7 usages de `.row` du dépôt (y compris un usage non mentionné dans le résumé initial, trouvé par grep — la liste détaillée du `WealthChart`), aucun artefact visuel aux coins malgré la combinaison `border-radius`+`border-bottom` ; aucune régression d'alignement ni de débordement sur aucune page. **Un point de cohérence documentaire signalé par le relecteur, pas un bug** : un commentaire de code affirmait à tort que ce fichier documentait déjà l'abandon de la table — corrigé (le commentaire décrit maintenant honnêtement le retour utilisateur de ce lot, sans prétendre qu'il était déjà écrit ailleurs) en même temps que la présente section, qui est précisément cette documentation manquante.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé — aucune fonction de domaine touchée par les trois passes), `pnpm run build` (réussi). Les scénarios `test:e2e` les plus pertinents (captures officielles, nom d'établissement long) rejoués individuellement par chaque auteur et par le relecteur, tous verts ; le run groupé reste intermittent dans ce bac à sable (limite déjà documentée ailleurs, pas une régression). Captures officielles régénérées et montrées à l'utilisateur à chaque étape avant publication, pas seulement après coup.

Fusionné dans `main` (commit `4fb4e3c`, PR #12). CI et GitHub Pages vérifiés verts sur ce commit, site public à jour. Montré à l'utilisateur après déploiement, qui n'a pas renouvelé de rejet cette fois et a demandé deux évolutions fonctionnelles précises sur « Les opérations » (Mon mois) — voir section suivante.

## Ajout — tri « pas payé d'abord » et « Voir les autres mois » sur Mon mois (développé et testé le 22 septembre 2026)

En réaction à la refonte visuelle publiée, l'utilisateur a demandé deux évolutions précises sur la carte « Les opérations » (confirmées via des questions à choix explicites, pas devinées) : que les opérations pas encore payées remontent en haut de la liste, et pouvoir voir tous ses mois sans re-sélectionner le sélecteur à chaque fois.

**Tri** : nouvelle fonction locale `sortOperations(list)` (`src/App.tsx`), tri stable `planned`/`unknown` avant `settled`, sans toucher à l'ordre par date à l'intérieur de chaque groupe (celui déjà renvoyé par `transactionsForMonth`, inchangé). Appliquée à la carte « Les opérations » de Mon mois.

**« Voir les autres mois »** : nouvel état `showAllMonths` (`useState(false)`, désactivé par défaut). `otherMonthsWithData` liste tous les mois distincts présents dans `data.transactions` (hors mois sélectionné), triés du plus récent au plus ancien — volontairement borné aux mois où une vraie transaction existe déjà, jamais déduit de la portée indéfinie d'une récurrence. Un bouton texte bascule l'état ; **le bloc supplémentaire n'est monté dans le DOM que si l'état est vrai** — délibéré : le monter par défaut aurait fait apparaître un même libellé de récurrence dans plusieurs `.row` à la fois, cassant le sélecteur strict-mode déjà utilisé par le scénario e2e `daily entries` (`.locator(".row", {hasText: "Assurance test"})` sans `.first()`). Chaque mois supplémentaire réutilise exactement `transactionsForMonth(data, m)` (fonction de domaine déjà existante, non modifiée), passée par le même tri et le même filtre par nature que le mois courant, sous un sous-titre `monthLabel(m)`.

Relu indépendamment par l'agent `finance-verification` (distinct de l'auteur), en conditions réelles (vrai formulaire, pas d'injection) : tri confirmé sur un mois avec 3 opérations non payées et 2 payées entrelacées (22/22 contrôles) ; « Voir les autres mois » vérifié avec 2 mois passés distincts créés via le formulaire (bouton absent puis présent avec le bon compte, sous-titres corrects, tri et filtres fonctionnels dans chaque groupe) ; « Marquer payé » depuis un mois passé confirmé sans doublon ni orphelin après enregistrement (l'opération rejoint logiquement le mois courant, `markSettled` fixant déjà `date: today()` pour une opération ponctuelle — comportement existant, pas introduit par ce lot) ; non-régression de `daily entries` reconfirmée avec un test ciblé supplémentaire du risque théorique documenté dans le code (1 `.row` correspondant avant clic sur le bouton, 2 après — preuve concrète que c'est bien le montage conditionnel qui protège le test, pas une coïncidence) ; aucun débordement à 390/834/1440 avec le bloc déplié ; aucune erreur console. Verdict « conforme, aucun défaut trouvé ».

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé — aucune fonction de domaine touchée, seulement une fonction de tri locale et un calcul de liste de mois dans `App.tsx`), `pnpm run build` (réussi), les scénarios `test:e2e` `daily entries` et `real rendered demo screenshots` rejoués individuellement par l'auteur et le relecteur, tous verts (le run groupé en parallèle reste intermittent dans ce bac à sable, limite déjà documentée, pas une régression).

Fusionné dans `main` (commit `91ad711`, PR #13). CI et GitHub Pages vérifiés verts sur ce commit, site public à jour.

## Simplification des lignes réglées sur Mon mois + corrections d'audit qualité (développé et testé le 22 septembre 2026)

L'utilisateur a demandé de simplifier « Les opérations » : une ligne déjà réglée (payée/reçue) affichait toujours ses deux icônes justificatif+modifier comme une ligne encore à traiter, ce qu'il a jugé chargé une fois qu'il a vu le tri « pas payé d'abord » en usage réel. Confirmé via une question à choix explicite (« Oui, retirer ces 2 icônes des lignes payées »), avec la précision que les deux fonctions restent accessibles autrement.

**Changement** (`transactionRow`, `src/App.tsx`) : sur une ligne réglée, l'icône crayon « Modifier » disparaît — `.row-main` (bloc titre/détail) devient lui-même la cible cliquable (`role="button"`, `tabIndex=0`, `aria-label="Modifier {label}"`, clic et clavier Entrée/Espace), avec une classe `.row-main-button` ajoutée en CSS (curseur + coin arrondi, s'appuyant sur le `:focus-visible` et le `.row:hover` déjà existants). L'icône justificatif, elle, **reste** affichée sur les lignes réglées : un premier essai l'avait aussi retirée, ce qui a fait échouer le scénario e2e `daily entries` (qui attache un justificatif directement depuis un revenu déjà marqué « Reçu ») — corrigé en restaurant la condition d'origine (`t.status !== "planned"`), inchangée depuis avant ce lot. Le bouton « Remettre à... » (récurrence réglée) n'est pas touché.

Relu indépendamment par `finance-verification` (distinct de l'auteur), en conditions réelles (build + preview, navigation clavier réelle, captures de focus) : comportement sur les lignes réglées confirmé conforme (icône crayon absente, justificatif présent, clic et clavier Entrée/Espace ouvrent l'éditeur, focus visuel net, aucun doublon d'`aria-label`, ligne réglée liée à une récurrence toujours correcte) — **un défaut réel trouvé** : l'icône justificatif était réapparue par erreur sur les lignes *planifiées* (hors périmètre demandé, contredisant un commentaire de code explicite sur la surcharge visuelle d'une ligne planifiée), corrigé par le coordinateur en restaurant la condition d'origine, puis revérifié (`pnpm run typecheck`, `pnpm run test` 135/135, `pnpm run build`, scénarios `daily entries` et `subscriptions` rejoués individuellement, tous verts).

**En parallèle**, un audit qualité plus large demandé par l'utilisateur (« développe tout au max, améliore encore ») a trouvé et corrigé trois défauts réels indépendants, mesurés sur le rendu effectif :
- `.brand` (logo dans la barre latérale) : cible tactile de 39px de haut, sous le plancher de 44px de l'app sur desktop et tablette — `min-height: 44px` ajouté.
- `.year-control .icon-button` (sélecteur d'année) : un override à 36×36 le ramenait sous le plancher 44×44 déjà respecté par `.icon-button` ailleurs, visible à côté des puces de mois à 44px — override supprimé.
- `.breadcrumb span` : couleur `#50596f` mesurée à 2,82:1 sur les pixels réellement composés contre le fond, sous le seuil AA de 4,5:1 — remplacée par le token `--subtle` déjà existant (5,39:1).

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**, inchangé), `pnpm run build` (réussi), captures officielles régénérées avec l'état final du code.

## Suite de l'audit qualité — focus clavier et code mort (vérifié le 22 septembre 2026)

Le même audit qualité (« développe tout au max, améliore encore »), poursuivi de façon plus approfondie par `finance-verification` sur l'état ci-dessus (build réel + preview, jeu de données stressé construit via les vrais formulaires : 8 comptes des 4 natures, 6 récurrences, 12 opérations sur 4 mois, 4 projets, 4 positions), a trouvé et corrigé deux défauts réels supplémentaires et nettoyé du code mort :

- **Focus clavier perdu à la fermeture d'une boîte de dialogue** (`src/components/Editor.tsx`) : un parcours clavier pur (Tab/Enter/Échap, sans souris) a montré que fermer l'éditeur laissait `document.activeElement` sur `<body>` au lieu de revenir au bouton déclencheur — un utilisateur clavier devait retabuler depuis le haut de la page. Cause : le parent démonte tout le sous-arbre de l'éditeur dans le même rendu qui retire le focus, avant que `dialog.close()` (l'effet de nettoyage) puisse s'exécuter sur un nœud encore attaché ; la restauration native de `<dialog>` n'a donc plus rien à cibler. Corrigé en captant l'élément actif au montage et en le refocalisant explicitement à la fermeture (patron WAI-ARIA : le focus revient au contrôle qui a ouvert la boîte de dialogue). **Limite documentée, non corrigée** : ce correctif couvre Annuler/Échap/Modifier, mais pas encore le cas où « Marquer payé » change le libellé du bouton de ligne lui-même après enregistrement — corriger ce cas précis demanderait de re-cibler la ligne par identifiant de transaction après sauvegarde, laissé pour un lot séparé plutôt que risqué sans seconde revue.
- **9 règles CSS mortes supprimées** (`src/index.css`) après recherche exhaustive de références (y compris dynamiques) : `.button.danger`, `.metric-row` (+ ses variantes en media query), `.donut-center`/`.donut-center strong`, `.file-input`, `.doc-grid` et ses sous-règles, `.allocation-bar`. CSS livré réduit de 32,12 à 30,77 kB. Aucun résidu de l'ancienne table « Mes comptes » trouvé (déjà nettoyé lors du retour aux cartes).
- Contrôles négatifs positifs (rien à corriger, vérifié et non simplement supposé) : 0 débordement horizontal sur 48 combinaisons page×format×jeu de données ; 0 échantillon de contraste sous AA après le correctif du fil d'Ariane (220 échantillons, texte composite compris) ; 0 cible tactile sous 44×44 après les deux correctifs précédents ; badges de nature de compte et icônes de récurrence confirmés identiques partout où ils apparaissent ; anneau de focus visible sur 25 arrêts de tabulation consécutifs.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**), `pnpm run build` (réussi, CSS 32,12→30,77 kB), `pnpm audit --audit-level high` (aucune vulnérabilité), les 9 scénarios `test:e2e` rejoués individuellement, tous verts (le run groupé en parallèle reste intermittent dans ce bac à sable, limite déjà documentée, pas une régression).

Fusionné dans `main` (commit `5b9bf29`, PR #14, deux commits). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Paiement instantané, édition rapide, simplification Abonnements et Mon mois (développé et testé le 22 septembre 2026)

En réaction à l'usage réel de la refonte précédente (captures annotées de croix et cercles rouges), l'utilisateur a demandé plusieurs simplifications concrètes en une série de messages : le bouton « Marquer payé » doit agir immédiatement, sans dialogue intermédiaire (« quand j'appuie sur le bouton payer il devient vert mais l'exécution est direct ») ; le crayon d'édition d'une ligne pas encore payée ne doit permettre de changer que le nom, le montant et le mois (« mais c'est tout ») ; les cartes « Le mouvement du mois » et « Projection nette » sur Mon mois sont de trop (croisées sur capture) ; la liste des abonnements doit devenir une carte longue avec juste nom/montant/mois ; le vocabulaire de la page Abonnements est trop technique (« les termes c'est trop, je veux des termes plus simples ») ; les champs de date sont visuellement moches ; le graphique de patrimoine doit être garanti connecté aux vrais soldes de compte.

Développé en parallèle : le coordinateur sur `src/App.tsx` (le fichier central, touché par presque toutes les demandes) pendant que trois agents `finance-designer`/`finance-patrimoine`/`finance-frontend` travaillaient chacun sur une partie disjointe (`src/index.css` pour le style des dates, vérification du graphique de patrimoine sans toucher à App.tsx, et `src/components/Editor.tsx` pour l'éditeur rapide).

1. **Paiement instantané** (`src/App.tsx`) : nouvelle fonction `quickSettle(t)` qui écrit directement statut « réglé » + date du jour (ajout ou remplacement dans `data.transactions`, même logique que `submit()` de l'éditeur) sans ouvrir de dialogue — remplace `markSettled`, qui ouvrait l'éditeur pré-rempli. Utilisée à la fois sur les lignes de transaction (Mon mois, « Prochaines échéances ») et sur les lignes d'abonnement. Un flash vert bref (`.row-flash-positive`, `src/index.css`, animation CSS auto-nettoyante via `onAnimationEnd`) confirme visuellement l'action puisqu'il n'y a plus de dialogue de confirmation.
2. **Éditeur rapide** (`src/components/Editor.tsx`, nouveau `EditorSpec.quick`) : le crayon d'une ligne pas encore payée ouvre un formulaire limité à Libellé/Montant/Mois ; tous les autres champs (compte, devise, catégorie, statut, date précise) sont préservés via des champs cachés portant leur valeur actuelle — rien n'est modifié silencieusement au-delà de ce qui est affiché. Changer uniquement le mois vide la date précise et fixe `budgetMonth` au mois choisi (la transaction change alors de mois) plutôt que d'inventer un jour qui n'a jamais été donné — même concept que `transactionsForMonth` (`date?.slice(0,7) ?? budgetMonth`). Le parcours complet (« Ajouter », et l'éditeur complet ouvert en cliquant une ligne déjà réglée) reste strictement inchangé.
3. **Mon mois** (`src/App.tsx`) : les cartes « Le mouvement du mois » et « Projection nette » sont retirées de cette page (celle de l'Accueil, dans un contexte différent aux côtés de « Répartition du patrimoine » et « Prochaines échéances », n'est pas touchée).
4. **Abonnements en carte longue** (`src/App.tsx`, `subscriptionRow`) : n'affiche plus que le nom, le mois sélectionné, le statut et le montant dû/réglé pour ce mois — la cadence, le jour du mois, le compte et l'équivalent mensuel sont retirés de l'affichage (toujours modifiables via le crayon, qui ouvre toujours l'éditeur complet de récurrence, inchangé).
5. **Vocabulaire simplifié sur Abonnements** (`src/App.tsx`) : le paragraphe « Cohorte d'échéances : ce qui est dû... Flux réalisé : ce qui a été réglé... » devient « Vos abonnements et charges pour {mois}. » ; les 6 étiquettes de statistiques sont renommées en termes plus simples (« Dû en {mois} » → « Dû ce mois », « Réglé pour {mois} » → « Réglé ce mois », « Récurrences actives » → « Abonnements actifs », « Payé en {mois} » → « Payé ce mois », « Reçu en {mois} » → « Reçu ce mois », « Reste dû » inchangé) — seuls les libellés changent, les 6 chiffres sous-jacents restent les mêmes calculs.
6. **Champs de date/mois restylés** (`src/index.css`) : icône du calendrier recolorée (invisible en noir sur fond sombre auparavant), `color-scheme: dark` ajouté ; le champ lui-même (fond/texte) était déjà correctement thématisé par héritage de `.field input`. Le popup calendrier natif du navigateur reste hors de portée du CSS au-delà de `color-scheme`.
7. **Graphique de patrimoine vérifié** (`src/components/Charts.tsx`, `src/domain/finance.ts`) : audit indépendant confirmant que « Patrimoine observé » fait déjà exactement ce qui était demandé — chaque point est un vrai solde daté réel (aucune interpolation), un nouveau solde ajouté à un compte apparaît immédiatement sans recharger la page, et plusieurs comptes à des dates différentes se fusionnent correctement (reconduction de la dernière valeur connue par compte, vérifiée au centime près par calcul manuel indépendant). Aucun changement de code nécessaire ; rien n'a été modifié dans ces fichiers.
8. **Retour instantané au clic/press** (`src/index.css`) : demande explicite (skill de conception « apple-design ») d'appliquer le principe « répondre à l'appui, pas au relâchement ». `.button`, `.icon-button`, `.nav-item`, `.card-action`, `.tab-button`, `.text-button` n'avaient jusqu'ici qu'un état `:hover` (invisible au clavier/tactile) — un état `:active` (réduction d'échelle ou opacité, ~100ms) leur est ajouté, cohérent avec le flash vert déjà livré sur `quickSettle`.
9. **Entrée en matière des boîtes de dialogue** (`src/index.css`, `@starting-style`) : une boîte de dialogue apparaissait jusqu'ici instantanément (aucune transition). Elle se matérialise désormais (fondu + léger zoom, 200ms) à l'ouverture. La fermeture reste instantanée : `Editor.tsx` démonte le composant de façon synchrone au clic sur Fermer/Annuler (avant même que `dialog.close()` ne s'exécute), donc une transition CSS pure n'a aucune fenêtre de temps pour s'exécuter à la fermeture — l'animer proprement demanderait de retarder le démontage dans `Editor.tsx`, un changement distinct, plus risqué, non fait ici.

Les points 8 et 9 sont un ajout CSS pur, à faible risque (aucune logique, aucun état touché), vérifiés par le coordinateur seul (build, 135/135 tests, capture d'écran réelle de l'ouverture, scénario e2e `daily entries` rejoué) plutôt que par une revue indépendante séparée — la revue indépendante dédiée porte sur les points 1 à 7 ci-dessus. Le mouvement réduit (`prefers-reduced-motion`) et la transparence réduite (`prefers-reduced-transparency`) étaient déjà couverts globalement avant ce lot (règle générale `* { transition-duration: 0.01ms !important }` etc.) et couvrent automatiquement ces nouveaux ajouts sans changement supplémentaire.

Relu indépendamment par `finance-verification` (distinct des auteurs), en conditions réelles (build + preview isolé sur un port dédié, export JSON non chiffré comme oracle indépendant comparé octet pour octet, ~15 scénarios ad hoc en plus des 9 scénarios officiels rejoués individuellement) : **trois défauts réels trouvés, tous corrigés puis revérifiés par le coordinateur** (`pnpm run typecheck`/`test`/`build` propres, `daily entries` et `subscriptions` rejoués verts) :
- **[Modéré]** `quickSettle` ne stampait pas `source.updatedAt`/`source.note` contrairement à `revertToPlanned` (le patron de référence) et à l'ancien `markSettled` (qui passait par `submit()` de l'éditeur, lequel stampe toujours « Modification manuelle »). Cassait l'invariant documenté ci-dessus (« Une information importée puis modifiée garde une indication de modification manuelle »), reproduit concrètement par le relecteur sur une dépense importée par CSV. Corrigé : `quickSettle` applique désormais le même stamp que `submit()`.
- **[Modéré]** Dans `subscriptionRow`, quand une récurrence active n'a aucune échéance le mois sélectionné (`cohortItem` nul), le montant affiché retombait sur celui de la *prochaine* échéance (un mois différent) juste à côté du texte « {mois} · Aucune échéance » — un montant sans rapport avec le mois affiché, exactement le risque que la carte longue devait éviter. Corrigé : aucun montant n'est affiché dans ce cas (cohérent avec « Aucune échéance »), `nextOccurrenceDate`/`recurrenceAmountAt` alors inutilisés retirés de `subscriptionRow`.
- **[Mineur]** Le flash vert ne se déclenchait jamais sur la carte « Prochaines échéances » de l'Accueil : cette liste filtre sur `status === "planned"`, donc la ligne en sortait avant que la classe d'animation n'ait pu s'appliquer (confirmé par sondage frame par frame). Atténué en pratique par le toast générique « Enregistré », mais la confirmation dédiée manquait bien à cet endroit. Corrigé : la ligne juste réglée reste visible le temps du flash (`|| t.id === justSettledId` ajouté au filtre), puis disparaît normalement une fois l'animation terminée.

Aucun défaut trouvé sur les points 3, 5 et 6 ; aucun défaut de fusion silencieux malgré la fusion à 3 voies décrite ci-dessous.

**Incident de coordination signalé par transparence** : les trois agents travaillant en parallèle sur des fichiers disjoints ont ponctuellement rencontré des états transitoires de `src/App.tsx` pendant que le coordinateur l'éditait (erreurs de compilation passagères observées par deux agents distincts, confirmées non liées à leur propre travail). Un agent a par ailleurs isolé temporairement `src/App.tsx` via `git stash` pour tester son changement seul, entrant en conflit avec les écritures concurrentes du coordinateur sur ce même fichier ; il a résolu ce conflit par une fusion à 3 voies (`git merge-file`) plutôt que d'écraser un des deux côtés. Le coordinateur a revérifié après coup, par recherche de chaque marqueur attendu dans le fichier final (`quickSettle`, `justSettledId`, le câblage `quick: true`, les nouvelles étiquettes, l'absence de code mort résiduel) puis par `typecheck`/`test`/`build` complets, qu'aucun des deux travaux n'avait été perdu par cette fusion.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**135/135**), `pnpm run build` (réussi, `dist/sw.js` généré), les 9 scénarios `test:e2e` rejoués individuellement, tous verts (le run groupé en parallèle reste intermittent dans ce bac à sable, limite déjà documentée, pas une régression). Vérification manuelle supplémentaire du parcours d'édition rapide (champs visibles limités à Libellé/Montant/Mois confirmés par lecture du DOM, montant modifié sans effet de bord sur les autres champs, changement de mois confirmé déplaçant réellement la transaction).

Fusionné dans `main` (commit `249c009`, PR #16). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Audit à sept agents — sécurité, calculs, patrimoine, design, import/migration, éditeur, couverture e2e (développé et testé le 23 septembre 2026)

Demande explicite de l'utilisateur : faire travailler un grand nombre d'agents en parallèle pour tout améliorer et tout auditer, plutôt qu'une nouvelle demande fonctionnelle précise. Comme pour le lot précédent (trois agents), faire travailler littéralement autant d'agents sur les mêmes fichiers centraux aurait provoqué des conflits sans bénéfice : sept agents ont été lancés sur des fichiers strictement disjoints, aucun ne committant lui-même, `src/App.tsx` restant réservé au coordinateur.

1. **`finance-securite` → `src/vault.ts`** : audit KDF (PBKDF2-SHA256, 600 000 itérations à la création, bornes `[< actuel, 1 000 000]` imposées à l'import), IV (12 octets frais à chaque `seal()`, jamais réutilisé), AAD (`additionalData()` lie `savedAt` et les métadonnées — une enveloppe altérée pour retirer `savedAt` et contourner le contrôle anti-régression casse l'authentification, vérifié explicitement), zéroisation des buffers, absence de fuite dans la console/le stockage/les messages d'erreur. Aucun défaut trouvé, aucune modification. Point signalé hors périmètre (voir point 8 ci-dessous).
2. **`finance-calculs` → `src/domain/finance.ts`, `validation.ts`** : audit récurrences (fins de mois, années bissextiles), résolution `date`/`budgetMonth`, arrondis (`convertMinor`/`roundRatio`, sans biais, vérifié algébriquement), transferts, doublons, validation. **Un défaut réel trouvé et corrigé** : `withRecurrenceAmount` archivait un `amountHistory` incorrect lors d'une double modification de montant le même jour, ou d'une correction antidatée ne dépassant pas un changement déjà archivé — pouvant produire une inversion chronologique (`recurrenceAmountAt` renvoyant l'ancien montant pour une date postérieure). Corrigé : une modification encore en attente (jamais réellement en vigueur) est désormais amendée sur place plutôt que ré-archivée ; revenir en arrière d'un changement déjà archivé lève maintenant une erreur explicite plutôt que produire un résultat incohérent. Trois tests de régression ajoutés.
3. **`finance-patrimoine` → `src/components/Charts.tsx`** : vérification demandée explicitement par l'utilisateur (graphique connecté aux vrais soldes, mis à jour à chaque changement de compte). Tracé complet du flux `data.accounts[].balances` → `wealthSummary`/`accountValue`/`latestBalance` → props de `WealthChart`, confirmé sans `useMemo` figé, sans donnée codée en dur, un point par date de relevé réelle (jamais un mois fabriqué), pas de double comptage. **Confirmé réellement connecté**, aucune modification nécessaire.
4. **`finance-designer` → `src/index.css`, `src/components/MonthPicker.tsx`** : redesign demandé explicitement (« regarde les dates c'est moche »). Root-cause identifiée sur captures réelles : texte des puces de mois coupé net aux bords du défilement, sélecteur d'année et rangée des mois lus comme deux boîtes séparées non alignées, état actif en gris plat sans identité de marque. Corrigé : `.month-nav-bar` unifie les deux en une seule surface, fondu en dégradé de masque CSS aux bords du défilement (`mask-image`, avec repli sans transparence sous `prefers-reduced-transparency`), état actif en dégradé bleu-violet cohérent avec `.nav-item.active`, cibles vérifiées 44×44px. **Un défaut réel de contraste trouvé et corrigé** en audit plus large : `.button.primary`/`:hover` mesurait 3.97:1/3.17:1 (sous la norme AA 4.5:1) — dégradés assombris à ~4.6–4.9:1, même teinte conservée.
5. **`finance-donnees-sync` → `src/importCsv.ts`, `src/domain/migration.ts`** : audit parsing RFC-4180, délimiteurs, montants, dates, doublons `externalId`, virements, borne 50 000 lignes, idempotence de migration, échec sûr (jamais de remplacement partiel). Aucun défaut réel trouvé, aucune modification ; une observation cosmétique (ordre des messages d'erreur) laissée telle quelle.
6. **`finance-frontend` → `src/components/Editor.tsx`, `Icon.tsx`** : audit accessibilité du dialogue (piège de focus, restauration du focus, `aria-describedby`), cohérence des champs entre mode complet et mode rapide, icônes réelles. Corrigé : 4 icônes mortes supprimées (`search`, `trash`, `moon`, `chevron-down` — aucune fonctionnalité correspondante n'existe dans l'application, facilement ré-ajoutables), logique de fusion date/mois du mode rapide extraite en fonction pure testable (`transactionDateFields`, 4 tests ajoutés), indices d'aide (`hint`) désormais liés aux champs via `aria-describedby` pour les lecteurs d'écran (n'étaient auparavant que visuels).
7. **`finance-verification` → `tests/finance.spec.ts`** : comblement des trous de couverture sur les quatre changements du lot précédent, jusque-là non testés : flash vert (`.row-flash-positive`) sur Mon mois et Abonnements, éditeur rapide (champs visibles limités, déplacement de mois sans date inventée), étiquettes simplifiées d'Abonnements, retrait des cartes sur Mon mois (présentes sur l'Accueil, non touchées). +91/-4 lignes, aucune régression sur les tests existants.
8. **Coordinateur → `src/App.tsx`** (fichier central réservé, non délégué) :
   - Passphrase en clair dans le state React (`Auth`, flux « restaurer une sauvegarde plus ancienne ») signalée par `finance-securite` : `olderBackup.password` était conservé dans un state visible d'un éventuel inspecteur React DevTools/extension malveillante, alors que la passphrase a déjà été authentifiée avec succès à ce stade. Corrigé : déplacée dans un `useRef` (`pendingOlderBackupPasswordRef`), effacé juste après usage (succès/échec) et sur annulation/nouvelle tentative.
   - **Défaut réel trouvé par le coordinateur en vérifiant le test ajouté par `finance-verification` sur Abonnements** : le flash vert n'y était en réalité jamais applicable. `subscriptionRow` compare `justSettledId` à `dueTxn?.id ?? r.id` — mais `dueTxn` est un objet transaction virtuel (`id: "{recurrenceId}:{dueDate}"`, non encore dans `data.transactions`) qui redevient `null` dès que la récurrence est réglée (`cohortItem.settled` devient vrai), au moment précis où `justSettledId` est posé sur cet id virtuel. Le repli sur `r.id` ne correspond alors plus jamais à `justSettledId`. Corrigé : nouvelle clé `settledTxn?.id ?? dueTxn?.id ?? r.id` — `settledTxn` (la transaction réelle une fois réglée) porte le même id que l'ancien `dueTxn` puisque `quickSettle` conserve `t.id` inchangé, donc la clé reste identique avant et après le règlement. Confirmé par le test e2e ajouté par `finance-verification`, qui échouait avant ce correctif et passe après.
   - Deux points mineurs signalés en cours d'audit, corrigés : le bouton « Annuler » de `Editor.tsx` restait cliquable pendant l'enregistrement (`disabled={busy}` ajouté) ; le libellé de mois (« septembre 2026 ») était affiché deux fois à côté du sélecteur de devise, une fois visuellement par `MonthPicker` et une fois en texte redondant — ce second affichage passe en `sr-only` (reste annoncé aux lecteurs d'écran via `aria-live`, disparaît visuellement).

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**, en hausse depuis 135 avec les tests ajoutés par `finance-calculs` et `finance-frontend`), `pnpm run build` (réussi, `dist/sw.js` généré), les 9 scénarios `test:e2e` — dont le nouveau test « flash vert Abonnements », qui a effectivement révélé le défaut ci-dessus avant d'être corrigé — rejoués individuellement avec le binaire Chromium préinstallé, tous verts. Le run groupé en parallèle continue de produire des plantages de navigateur intermittents et non déterministes dans ce bac à sable (`--single-process`, ressources limitées) ; chaque échec observé a été rejoué seul et confirmé sans rapport avec le code, limite déjà documentée dans ce fichier, pas une régression de ce lot.

Fusionné dans `main` (commit `04ad766`, PR #18). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Dates lisibles sur le graphique de patrimoine + accent sur les onglets de filtre (développé et testé le 23 septembre 2026)

L'utilisateur a envoyé trois captures annotées (entourées en rouge) demandant d'améliorer le visuel de la barre mois/devise et de la carte « Patrimoine observé » sur l'Accueil, des statistiques sur Abonnements, et des onglets de filtre + liste des opérations sur Mon mois.

Deux de ces captures montraient en réalité l'**ancienne version en cache** : le sélecteur de mois en gris plat (déjà remplacé par l'accent bleu-violet dans le lot précédent) et le texte technique « Cohorte d'échéances » sur Abonnements (déjà simplifié). Vérifié directement dans le code courant avant tout changement plutôt que de re-développer à l'aveugle ; signalé à l'utilisateur que la bannière « Recharger » du service worker doit être acceptée pour voir la version courante.

Deux défauts visuels réels sont restés, une fois la confusion de cache écartée :

1. **Dates brutes sur le graphique** (`src/components/Charts.tsx`) : le pied du graphique « Patrimoine observé » et le détail « Voir les valeurs exactes » affichaient les dates au format ISO brut (`2026-04-28`), repérable sur la capture Accueil. Nouvelle fonction `dateLabel(date)` (`src/domain/finance.ts`), même convention que `monthLabel` déjà en place (locale `fr-CH`, `timeZone: "UTC"` pour éviter tout décalage de jour) — affiche désormais « 28 avr. 2026 ».
2. **Onglets de filtre en gris plat** (`src/index.css`) : les onglets Tout/Revenus/Dépenses/Virements sur Mon mois (entourés sur la capture) utilisaient encore `.tab-button.active { background: #262d41 }` — exactement le même gris plat que le sélecteur de mois avait déjà remplacé par l'accent bleu-violet dans le lot précédent (le commentaire de code de ce correctif signalait déjà explicitement ce gris comme « réutilisé par les groupes d'onglets génériques »). `.tab-button.active` reprend maintenant le dégradé exact de `.month-chip.active`, pour une sélection cohérente dans toute l'app (tous les groupes d'onglets partagent cette classe).

Vérifié visuellement (pas seulement lu dans le code) : preview isolé sur un port dédié, capture Playwright réelle confirmant « 28 avr. 2026 »/« 23 sept. 2026 » sous le graphique et le nouvel accent sur l'onglet « Tout » actif.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**), `pnpm run build` (réussi), les 9 scénarios `test:e2e` rejoués individuellement, tous verts (le run groupé reste le flake connu déjà documenté, pas une régression).

Fusionné dans `main` (commit `b47b1e3`, PR #20). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Coins plus carrés dans toute l'app (développé et testé le 23 septembre 2026)

Après les deux lots de polish visuel précédents, demande explicite de l'utilisateur : « plus jolie plus carré ». Plutôt que de deviner un élément isolé à corriger, passe systématique sur `src/index.css` : `--radius` (variable racine utilisée par `.hero-card`/`.card`) passe de `24px` à `16px`, et chaque autre `border-radius` non circulaire du fichier — boutons, onglets, lignes d'opérations, icônes de ligne/carte/établissement, sélecteur de mois, cartes de compte et de statistique, pilules, dialogue, champs de formulaire, écran de connexion, barre et menu de navigation mobile — est réduit dans la même proportion, y compris leurs variantes déclarées dans les media queries (900px/600px), pour une cohérence complète plutôt qu'un seul composant retouché. Les cercles et pilules réellement ronds (avatar, point de statut, barre de progression, donut, `kind-badge`) ne sont volontairement pas touchés : les « carrer » casserait leur forme plutôt que de les rendre plus géométriques.

Vérifié visuellement (pas seulement par lecture du code) via des captures Playwright réelles à 390px/834px/1440px sur Accueil et Mon mois, avant et après le changement.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**), `pnpm run build` (réussi), les 9 scénarios `test:e2e` rejoués individuellement, tous verts (le run groupé reste le flake connu déjà documenté, pas une régression).

Fusionné dans `main` (commit `14eccee`, PR #22). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Boutons payer/reçu colorés, cartes Abonnements/Investissements, refonte du sélecteur de mois, palette bleu néon (développé et testé le 23 septembre 2026)

Cinq nouvelles captures annotées et une demande explicite de faire travailler plusieurs agents en parallèle. Développé avec deux agents sur `src/index.css` (palette de couleurs et refonte du sélecteur de mois, sur des sélecteurs strictement disjoints) pendant que le coordinateur traitait `src/App.tsx`.

1. **Boutons « Marquer payé/reçu/réglé »** (`src/App.tsx`) : simplifiés en « Payer » (rouge, `--negative`), « Reçu » (vert, `--positive`), « Régler » (neutre) — le libellé et la couleur reflètent directement l'action sans le préfixe « Marquer ». Nouvelles classes `.button.pay`/`.button.receive` (`src/index.css`) réutilisant les tokens sémantiques déjà en place, pas de nouvelle couleur inventée.
2. **Cartes Abonnements et Investissements** (`src/App.tsx`) : chaque ligne (récurrence, position) devient une carte encadrée (bordure, dégradé, ombre — nouvelle classe `.item-card`, réutilise le langage visuel de `.account-card`) au lieu d'une simple ligne de liste divisée. Répond directement à la demande d'une « nouvelle page facture » façon carte : clarifié qu'il s'agit d'une amélioration visuelle de la page Abonnements existante (nom/montant/mois, récurrence mensuelle automatique) plutôt qu'une page séparée dupliquant les mêmes données.
3. **Sélecteur de mois repensé** (`src/components/MonthPicker.tsx`, agent `finance-frontend`) : le bandeau fusionné (année + 12 puces défilantes, jugé « pas jolie/pas adéquat ») est remplacé par un bouton compact (« Septembre 2026 ») ouvrant un panneau avec navigation d'année et grille 4×3 des mois — pattern WAI-ARIA Disclosure fait main, aucune dépendance ajoutée. Le panneau utilise `var(--accent)`/`var(--violet)` plutôt que des couleurs codées en dur, pour rester synchronisé avec la palette sans toucher au fichier de l'agent couleur. Comportement changé délibérément : naviguer les années dans le panneau ne valide plus immédiatement (contrairement à l'ancien bandeau) — seul un clic sur un mois valide, referme le panneau et redonne le focus au déclencheur.
4. **Palette bleu néon** (`src/index.css`, agent `finance-designer`) : `--accent` (`#8aa9ff` → `#0a84ff`) et `--violet` (`#aa96ff` → `#8f6bff`) resserrés vers un bleu plus vif et plus saturé, ainsi que tous leurs doublons codés en dur (boutons, onglets actifs, badges, icônes, lueurs de carte) — contraste AA vérifié (~5–7:1). `MONOGRAM_PALETTE` (`src/App.tsx`, tableau JS et non variable CSS) mis à jour en miroir par le coordinateur pour rester synchronisé.
5. `tests/finance.spec.ts` : le test dédié au sélecteur de mois est entièrement réécrit pour la nouvelle interaction (ouverture du panneau, navigation d'année sans validation immédiate, sélection qui referme/valide/redonne le focus, Escape).

Vérifié visuellement (pas seulement lu dans le code) via captures Playwright réelles : boutons colorés, cartes Abonnements/Investissements, panneau du sélecteur de mois ouvert/fermé, nouvelle palette sur Accueil et Mon mois.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**), `pnpm run build` (réussi), les 9 scénarios `test:e2e` — dont le test du sélecteur de mois réécrit — rejoués individuellement, tous verts (le run groupé reste le flake connu déjà documenté, pas une régression).

Fusionné dans `main` (commit `b5b8e74`, PR #24). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Correctif — focus clavier perdu après un règlement instantané (corrigé et testé le 23 septembre 2026)

Limite documentée depuis le lot « Paiement instantané » (voir « Prochaine action » ci-dessous, historique) : le bouton « Payer »/« Reçu »/« Régler » disparaît de la ligne dès qu'elle est réglée (remplacé par `row-main` cliquable ou par les icônes de ligne réglée), donc l'élément qui avait le focus clavier est retiré du DOM — le navigateur ramène alors le focus sur `<body>`, sans rien indiquer où continuer au clavier.

Corrigé dans `src/App.tsx` : chaque ligne (`transactionRow`, `subscriptionRow`) porte désormais un `data-row-id` stable (l'id de la transaction réglée, inchangé par `quickSettle`) et `tabIndex={-1}` (repère de secours programmatique, hors de l'ordre de tabulation normal). Un nouvel effet déclenché par `justSettledId` retrouve la ligne par cet id une fois le DOM à jour et déplace le focus sur son premier contrôle réel (le crayon, l'icône de reçu, ou `row-main` devenu cliquable), avec la ligne elle-même en repli. Ne se déclenche que si le focus est effectivement tombé sur `<body>` — ne vole jamais un focus que la personne a déplacé entre-temps.

Vérifié à la fois par script Playwright indépendant (transactionRow sur Mon mois **et** subscriptionRow sur Abonnements, `document.activeElement` confirmé hors de `<body>` et dans la ligne concernée après le clic) et par une nouvelle assertion e2e ajoutée au scénario « daily entries » existant.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**), `pnpm run build` (réussi), les 9 scénarios `test:e2e` — dont le nouveau contrôle de focus — rejoués individuellement, tous verts (le run groupé reste le flake connu déjà documenté, pas une régression).

Fusionné dans `main` (commit `e75716d`, PR #26). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Refonte visuelle plate et neutre — abandon du dégradé bleu-violet néon (développé et testé le 23 septembre 2026)

Retour explicite de l'utilisateur avec une image de référence (mockup de wallet multi-devises, fond noir plat, cartes grises unies, accent unique menthe/sauge, boutons ronds pleins) : l'app était trop chargée (halos radiaux colorés derrière quasi chaque carte, doubles voire triples accents colorés superposés sur une même carte de compte — fond dégradé + badge + icône). Demande explicite : auditer toute l'application et rapprocher tous les visuels de cette référence, « quelque chose de neutre, simple, efficace ».

Développé par `finance-designer` (`src/index.css`, `src/components/Charts.tsx`), intégré et complété par le coordinateur (`src/App.tsx`, les trois SVG du logo) :

- **Fonds de carte aplatis** : tous les `radial-gradient`/`linear-gradient` décoratifs retirés de `.hero-card`, `.card`, `.account-card`, `.stat-card`, `.item-card.row`, l'écran de connexion (`.auth-screen`/`.auth-card`) et le reste — chaque carte de données utilise désormais un unique fond plat (`--surface`/`--surface-raised`), distinguée du fond de page par la seule luminosité. Le verre (`backdrop-filter`) reste réservé à la navigation et aux commandes flottantes (`.dialog`, `.month-picker-panel`, `.mobile-nav`, `.toast`), conformément à `identite-ui.md`.
- **Ombres allégées** : `--shadow` passe d'un halo à deux couches + reflet inset à une ombre quasi invisible sur toutes les cartes de données.
- **Nouvel accent unique** : le duo `--accent`/`--violet` bleu-violet néon est remplacé par une seule teinte menthe/sauge (`--accent: #7fcba8`, `--accent-strong: #63b892`), choisie dans la même famille que `--positive` (`#8adebc`) déjà présente plutôt qu'une couleur inventée. `--violet` conservé comme alias de compatibilité (même valeur) pour qu'aucun `var(--violet)` existant ne casse. Contraste vérifié par calcul : `#7fcba8` sur `--bg` ≈ 10.4:1, sur `--surface` ≈ 9.2:1, texte du bouton primaire sur l'accent ≈ 10.8:1 — large marge sur le seuil AA (4.5:1). Tous les hex bleu-violet dupliqués en dur (liens, focus, sélecteur de mois, onglets actifs, nav, courbe de patrimoine, barres du mouvement du mois) migrés vers le nouveau token ; `grep` final confirme zéro occurrence résiduelle.
- **Badges de nature de compte désaturés** : `kind-badge-*`/`institution-icon-*` (banque/investissement/épargne/dette) resserrés vers une palette moins saturée, aucun ne réutilise `--accent` (réservé à la sélection/action), contraste ~7.3–8.3:1. La puce d'icône neutre par défaut (`.card-icon`/`.row-icon`), auparavant teintée bleu-violet sur presque toutes les cartes/lignes, passe à un gris neutre.
- **Rayons plus généreux** : `--radius` 16px → 22px (et les rayons spécifiques des cartes/boutons), pour un rendu plus doux conforme à la référence — ceci **renverse** une décision antérieure documentée (« coins plus carrés », lot précédent), assumé comme une demande utilisateur plus récente et plus explicite.
- **`Charts.tsx`** : la courbe du graphique de patrimoine (dégradé bleu→violet) devient un trait plat couleur accent ; les barres « Le mouvement du mois » (bleu/violet arbitraires) reprennent les tokens sémantiques déjà existants `--positive`/`--negative` (entrées en vert, dépenses en rouge, cohérent avec « Payer »/« Reçu » ailleurs) ; la palette catégorielle du donut « Répartition du patrimoine » resserrée dans la même famille neutre.
- **Logo** (`public/finance.svg`, `public/favicon.svg`, `public/finance-maskable.svg`) : le glyphe « F » et le fond « verre » recolorés de la même famille bleu-violet vers la nouvelle famille menthe/neutre, pour rester cohérents avec le reste de l'app (repéré par le relecteur comme oubli du premier passage, corrigé par le coordinateur).
- **`MONOGRAM_PALETTE`** (`src/App.tsx`) mise à jour en miroir des nouveaux tokens (accent menthe + les trois teintes `kind-badge-*` resserrées), puisque ce tableau est du TS pur et non des variables CSS.
- Balayage systématique confirmé page par page : Accueil (patrimoine, courbe, « Votre mois », aperçu du mois, comptes, donut, mouvement du mois, prochaines échéances), Mon mois, Mes comptes, Abonnements/Investissements, Épargne et projets, Documents et réglages, nav mobile, sélecteur de mois, écran de connexion/coffre.
- Couleurs sémantiques `--positive`/`--negative`/`--warning` (vert/rouge/orange) et les boutons `.button.pay`/`.button.receive` inchangés — le statut payé/reçu/à payer garde son sens.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**), `pnpm run build` (réussi), les 9 scénarios `test:e2e` rejoués individuellement, tous verts (le run groupé — cette fois avec un ensemble de 4 tests différent de d'habitude — reste le flake connu déjà documenté : resource contention Chromium `--single-process` dans ce bac à sable, pas une régression ; confirmé en isolant chaque test avec `--workers=1`). Captures `docs/captures/*.png` régénérées par le scénario e2e dédié, revues visuellement par le coordinateur (rendu plat confirmé, plus aucun halo coloré, logo cohérent avec la nouvelle palette).

Fusionné dans `main` (commit `c4f9302`, PR #28). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Correctif — bouton d'action désaligné + tri des opérations par nature et montant (corrigé et testé le 23 septembre 2026)

Retour utilisateur avec une capture annotée de Mon mois : le bouton « Payer » d'une ligne d'opération ne démarrait pas au même endroit d'une ligne à l'autre, et l'ordre des opérations ne suivait ni leur nature ni leur montant.

**Cause du désalignement** (`src/App.tsx`, `transactionRow`) : le crayon « Modifier » ne s'affiche que pour une opération planifiée déjà persistée (une occurrence de récurrence pas encore réglée n'a pas encore de transaction propre à éditer) — deux lignes « pas encore payé » pouvaient donc afficher un bouton seul ou bouton + crayon selon leur origine. Le crayon était rendu *après* le bouton dans `.row-actions` (flex, ancré à droite via `.row-end`), donc le bord droit du bouton reculait d'une ligne à l'autre selon qu'un crayon suivait ou non. Corrigé en rendant le crayon (et l'icône « remettre à prévu ») *avant* le bouton d'action au lieu d'après : le bouton est désormais toujours le dernier élément de `.row-actions`, son bord droit reste fixe quelle que soit la présence du crayon.

**Tri des opérations** (`sortOperations`, Mon mois) : conserve le tri « pas encore réglé d'abord » déjà existant, puis trie chaque groupe de statut par nature — reçu (revenu) d'abord, puis facture, puis abonnement, puis virement — et à l'intérieur de chaque nature, par montant décroissant (le plus gros d'abord), comme demandé explicitement. La nature d'une opération liée à une récurrence reprend le `recurrenceType` de cette récurrence (`subscription` → abonnement, tout le reste → facture) ; une opération manuelle sans récurrence est classée facture par défaut.

Nouveau test e2e dédié (`Mon mois: reçu, facture, abonnement, virement — montant décroissant dans chaque groupe, bouton toujours aligné`) : construit un jeu d'opérations couvrant les quatre natures avec des montants distincts, vérifie l'ordre exact des lignes, et compare la position (bounding box) du bouton « Payer » entre une ligne avec crayon et une ligne sans — même bord droit dans les deux cas.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**), `pnpm run build` (réussi), les 10 scénarios `test:e2e` — dont le nouveau — rejoués individuellement, tous verts (le run groupé reste le flake connu déjà documenté : resource contention Chromium `--single-process` dans ce bac à sable, un ensemble différent de tests échoue à chaque run groupé, jamais les mêmes deux fois de suite — pas une régression).

Fusionné dans `main` (commit `e1e45d1`, PR #31). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Correctif — montant coloré par nature sur toutes les listes d'opérations (corrigé et testé le 23 septembre 2026)

Retour utilisateur avec une capture annotée : le montant d'une dépense déjà payée (« Loyer », « Courses du quotidien ») restait en texte blanc neutre, sans lien visuel avec le fait qu'il s'agit d'une sortie d'argent — seul le revenu (« Salaire ») était coloré (vert). L'icône de la ligne, elle, était déjà rouge pour une dépense (`.row-icon` coloré par `t.kind` depuis un lot antérieur) : le montant ne suivait pas cette même règle.

Corrigé dans `src/App.tsx`, aux trois endroits où un montant d'opération datée (revenu/dépense) s'affiche en `.row-value`, pour que la couleur suive la nature de l'opération plutôt que son seul statut — donc valable aussi bien avant qu'après règlement, cohérent avec `.row-icon` qui fait déjà ça :

- `transactionRow` (Mon mois) : dépense → rouge (`negative`), en plus du vert déjà existant pour un revenu ; un virement reste neutre.
- `subscriptionRow` (Abonnements, aperçu Accueil) : n'avait aucune couleur de montant du tout jusqu'ici — ajouté, revenu récurrent → vert, dépense récurrente → rouge (un abonnement/une facture n'a pas de nature « virement »).
- Aperçu « Abonnements et charges récurrentes » sur Accueil (liste indépendante de `subscriptionRow`, même donnée) : même correctif.

Non touché intentionnellement : les positions d'investissement (`.row-value` sur Investissements) n'ont pas de nature revenu/dépense, donc restent neutres — pas de couleur inventée là où le concept ne s'applique pas.

Le test e2e du lot précédent (tri par nature/montant) est étendu avec une assertion de classe CSS (`positive`/`negative`) sur les trois natures présentes dans son jeu de données (revenu, dépense, virement), plutôt qu'un nouveau test séparé.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**), `pnpm run build` (réussi), les 10 scénarios `test:e2e` — dont le test étendu — rejoués individuellement, tous verts (run groupé toujours sujet au même flake connu de ce bac à sable, un ensemble différent de tests à chaque fois, jamais une régression réelle). Vérifié visuellement par capture réelle sur Mon mois et Accueil : dépenses en rouge qu'elles soient payées ou non, revenu en vert, virement neutre, cohérent partout.

Fusionné dans `main` (commit `538171a`, PR #33). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Refonte visuelle « verre + bleu néon » — lot 1/3 : tokens, boutons, navigation, écran de connexion (développé et testé le 23 septembre 2026)

L'utilisateur a fourni 4 captures de référence (tableaux de bord génériques, pas l'univers finance) montrant un langage visuel différent de la refonte plate/menthe livrée juste avant : fond sombre avec halo bleu diffus, cartes en verre translucide à bordure en dégradé, boutons pilule en dégradé bleu avec lueur, navigation translucide à onglet actif lumineux. Demande explicite : appliquer ce langage à « chaque page, chaque interface », avec un seul accent bleu (pas de duo bleu-violet, précisément retiré au lot précédent). Ampleur du changement traitée via le mode plan (deux agents d'exploration, deux clarifications utilisateur, un plan détaillé avec calculs de contraste WCAG AA réels avant toute écriture) puis découpée en 3 PR séquentielles pour isoler le calcul de contraste le plus sensible avant de le dupliquer sur les 7 pages. Ce lot est la première des trois : tokens, boutons, navigation, écran de connexion — développé par `finance-designer`, seul `src/index.css` modifié (`App.tsx`/`Charts.tsx` non touchés, aucune dépendance ajoutée).

**Palette** : `--accent`/`--accent-strong`/`--violet` (menthe) remplacés par une famille bleue unique (`--accent #4f8cff`, `--accent-strong #79b0ff`). Rôles explicitement séparés après vérification par calcul : `--accent-strong` est réservé au texte (`a:hover`, `.card-action:hover` le lisaient déjà comme couleur de texte, pas seulement comme fond de bouton — utilisé en fond de bouton il tombe à 2.22:1, échec net) ; `--accent-button-to`/`--accent-button-from` sont réservés au fond du bouton primaire uniquement. Nouveaux tokens `--accent-glow*`, `--glass-bg*`, `--glass-blur`, `--radius-pill`. `--positive`/`--negative`/`--warning` et `--radius` inchangés — la couleur de statut (revenu/dépense) reste indépendante de l'accent interactif.

**Technique de verre** : deux classes utilitaires définies (`.glass-card` avec flou réel, `.glass-row` même bordure en dégradé sans flou pour les listes denses), bordure en dégradé par double `background` `padding-box`/`border-box` — pas de `border-image` ni de pseudo-élément, pour la compatibilité Safari et le coût de paint. Ces classes ne sont pas encore appliquées aux cartes/lignes existantes (lot 2/3).

**Composants retouchés** : fond de page (double halo radial bleu, `background-attachment: fixed`) ; `.button` en pilule, `.button.primary` en dégradé bleu avec lueur (texte blanc, 4.91:1 à 6.69:1 selon le point du dégradé) ; `.button.secondary` en verre sans flou ; sélecteur de mois (déclencheur en pilule bordée, panneau reteinté, cellule active en aplat bleu plutôt que l'ancien `color-mix()` qui tombait à 3.88–4.16:1 avec le nouvel accent) ; navigation (`.nav-item.active` dégradé + lueur, `.sidebar` reçoit le verre pour la première fois — elle ne l'avait pas avant malgré la description initiale, `.mobile-nav .active` corrigé pour utiliser `--accent-strong` plutôt que `--accent` comme texte, l'association initialement prescrite échouant à 3.15–3.80:1) ; écran de connexion (`.auth-screen` ne masque plus le halo de fond, `.auth-card` en verre complet, `.auth-card input` avec un fond sombre dédié plutôt que le remplissage translucide de `.glass-row` qui aurait fait chuter le contraste du texte saisi, `.vault-mark` reteinté — il était en menthe codée en dur, indépendante du token).

Deux défauts réels trouvés par une vérification de contraste indépendante avant toute écriture (une première tentative de l'agent, restée en mode plan, a fait cette vérification sans coder) ont été corrigés dans le plan puis implémentés directement : la paire `.mobile-nav .active` et le rôle texte/fond de `--accent-strong`, tous deux détaillés ci-dessus. Un écart factuel (l'état supposé de `.sidebar`) a aussi été corrigé avant l'implémentation.

Hors périmètre, laissé pour le lot 2/3 : `.notice` (bandeau d'info) et le logo `.brand-mark` restent en menthe — ce ne sont pas des composants de navigation/commande, et une carte de contenu comme `.notice` relève du lot d'application des classes verre.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**), `pnpm run build` (réussi), les 10 scénarios `test:e2e` rejoués individuellement, tous verts (le run groupé a échoué sur 4 tests différents des lots précédents — même flake connu de ce bac à sable, resource contention Chromium `--single-process`, confirmé sans régression par une reprise individuelle de chacun). Contrastes recalculés par la formule de luminance relative WCAG officielle (pas estimés) sur chaque nouvelle paire couleur/dégradé, détaillés dans le rapport de l'agent. Captures Playwright réelles aux trois largeurs (1440/834/390) sur écran de connexion (avec focus clavier), tableau de bord, sélecteur de mois ouvert et navigation mobile : verre lisible, lueur bleue nette sur les états actifs, cellule de mois sélectionnée en aplat bleu contrasté, revenu/patrimoine restent verts (`--positive`, non touché) — vérifiées visuellement par le coordinateur avant fusion.

Fusionné dans `main` (commit `c592af5`, PR #35). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Refonte visuelle « verre + bleu néon » — lot 2/3 : cartes, lignes, formulaires, glow des icônes sur les 7 pages (développé et testé le 23 septembre 2026)

Deuxième lot de la refonte : applique les deux classes utilitaires `.glass-card`/`.glass-row` (définies mais pas encore utilisées au lot 1) aux vraies classes de cartes/lignes/formulaires, sur les 7 pages de l'app. Développé en deux passes séquentielles (pas en parallèle, pour éviter le risque d'édition concurrente sur un même fichier partagé, déjà rencontré une fois plus tôt dans cette session) : `finance-designer` sur `src/index.css` puis `finance-identite-visuelle` juste après sur les mêmes fichiers, suivi du câblage `App.tsx` par le coordinateur.

**Cartes conteneurs** (`.card`, `.hero-card`, `.stat-card`, `.dialog`, `.account-card`, `.goal-card` — hérite de `.card` via son `className` déjà combiné dans `App.tsx`) → `.glass-card` (verre complet, flou réel). `.account-card` était un cas limite explicitement laissé au profilage réel plutôt qu'à la supposition : vérifié par un test de densité synthétique (jusqu'à 15 cartes simulées sur Mes comptes, sans donnée financière inventée — le même compte cloné pour observer uniquement le rendu), aucune dégradation de lisibilité ni signal de coût GPU anormal (métriques CDP), donc gardé en verre complet plutôt que replié vers la variante sans flou.

**Lignes répétées densément** (`.item-card.row` — Abonnements, Investissements) → bordure en dégradé façon `.glass-row`, sans flou propre (évite un flou empilé sous celui de la carte parente déjà en verre).

**Champs de saisie** (`.field input/select/textarea`) → même technique de bordure, mais avec le remplissage sombre déjà validé pour `.auth-card input` (pas le film blanc translucide de `.glass-row`) : un cas de « verre empilé sur verre » vérifié par calcul avant usage — le film blanc aurait fait tomber le contraste du placeholder à 4.38:1 dans une boîte de dialogue déjà en verre, sous le seuil AA. Focus : glow bleu au lieu d'un simple changement de couleur de bordure, même motif qu'au lot 1.

**Glow d'icônes au survol du parent** (`.card-icon`, `.row-icon`, `.institution-icon`) : liseré discret au repos, lueur bleue seulement quand la carte/ligne parente est survolée ou reçoit le focus — jamais un glow permanent sur chaque icône, pour ne pas reproduire le défaut « trop chargé » déjà corrigé une fois dans ce projet (fond dégradé + badge + icône tous colorés en même temps). Volontairement limité à `border-color`/`box-shadow`, jamais `color`, pour ne pas interférer avec la teinte propre des badges `.institution-icon-*`/`.kind-badge-*`.

**Collision de teinte corrigée** (`finance-identite-visuelle`) : `.kind-badge-bank`/`.institution-icon-bank` utilisaient déjà un bleu (`#8fabc9`) trop proche du nouvel accent (`#4f8cff`) — un badge « compte bancaire » qui se confondrait avec l'accent interactif, contraire à la règle déjà documentée du projet. Reteinté vers un slate-teal distinct (`#89bec2`), contrastes recalculés par la formule WCAG (texte 6.6–7.7:1, icône ≥3:1).

**Nouvel interrupteur** : le bouton « Masquer/Afficher les montants » (`App.tsx`) passe d'un `icon-button` à icône œil à un vrai interrupteur piste + curseur (`.toggle`, nouvelle classe CSS avec glow bleu à l'état actif) — `className="toggle"` + `aria-pressed={hidden}`, icône retirée (redondante avec la position du curseur). Vérifié visuellement aux deux états.

Écart de contraste documenté et non corrigé, accepté tel quel : le texte `--accent` (liens `.card-action`) sur `.glass-card`, uniquement quand la carte se trouve directement sous le halo de fond ambiant en cours de défilement (jamais au repos), mesure ≈4.51:1 — passe le seuil AA mais avec une marge fine plutôt que confortable. Non corrigé unilatéralement pour ne pas désynchroniser le token partagé `body` du lot 1 ; à vérifier sur une vraie capture en défilement par une future passe de vérification.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run build` (réussi), `pnpm run test` (**142/142**), les 10 scénarios `test:e2e` rejoués individuellement, tous verts (run groupé de nouveau sujet au même flake connu de ce bac à sable — un ensemble différent de tests à chaque fois, confirmé non régression par reprise individuelle). Captures Playwright réelles revues sur Accueil, Mes comptes (à densité), Abonnements, Épargne et projets, une boîte de dialogue ouverte, et le nouvel interrupteur aux deux états : verre lisible partout, aucune carte surchargée, couleurs de statut (`--positive`/`--negative`) inchangées.

Fusionné dans `main` (commit `c329771`, PR #37). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Refonte visuelle « verre + bleu néon » — lot 3/3 : six éléments visuels basés sur des données réelles (développé et testé le 23 septembre 2026)

Dernier des 3 lots. Contrairement aux deux précédents (CSS seul), celui-ci touche `src/App.tsx` et `src/components/Charts.tsx` — développé personnellement par le coordinateur plutôt que délégué, conformément au plan : c'est le seul des 3 lots avec un vrai risque de correction de donnée, il méritait sa propre revue directe plutôt qu'un passage par agent. Chacun des six éléments réutilise une valeur déjà calculée ailleurs dans l'app ; aucune donnée n'est inventée.

1. **Aire en dégradé sous la courbe de patrimoine** (`WealthChart`, `Charts.tsx`) : réutilise le tableau `segments` déjà construit pour la ligne, fermé jusqu'à la ligne de base (`y=126`) en un second `<path>` avec un `<linearGradient>` bleu → transparent. Corrige au passage un vrai défaut trouvé en relisant le fichier : la ligne et les points de la courbe restaient codés en dur sur l'ancien mint (`#7fcba8`), un reliquat de la passe neutre d'avant cette refonte que le lot 1 (qui n'a touché que `index.css`) n'avait pas pu corriger puisque cette couleur est écrite directement dans le SVG, pas via `var(--accent)` — recolorée en bleu (`#4f8cff`) avec ce lot.
2. **Barre de progression des objectifs** (Épargne et projets) : option la plus simple retenue, `.progress-fill` passe d'un aplat `var(--accent)` à un dégradé lumineux avec `box-shadow`, zéro changement JSX — la variable `progress` déjà calculée (`reservedMinor/targetMinor`) n'est pas touchée.
3. **Mini-courbe par compte** (`.account-card`) : construite à partir de l'historique réel `a.balances` (le même tableau que lit déjà `latestBalance()`), uniquement quand au moins deux soldes ont à la fois un montant et une date — un seul solde d'ouverture ou une entrée non datée ne dessine rien plutôt qu'une tendance fabriquée.
4. **Part du patrimoine par compte** : compose `rankAccounts` (déjà utilisé pour le tri) et `wealthSummary.totalMinor` (déjà utilisé pour le total affiché) en un simple ratio, sans nouvelle fonction de domaine. Affichée sur chaque carte de compte ; vérifiée visuellement cohérente avec le donut « Répartition du patrimoine » de l'Accueil qui affiche déjà les mêmes pourcentages (49.4 %, 37.4 %, 13.2 % sur les données de démonstration). Une dette peut afficher une part négative — assumé, pas masqué : elle retire réellement de la valeur au total.
5. **Progression « réglé ce mois »** (Abonnements) : ratio `settledMinor/dueMinor` de `cohortSummary`, déjà affiché en toutes lettres dans les cartes de statistiques juste au-dessus. N'apparaît que lorsque `dueMinor > 0` — à 0 dû, une barre à 0/0 lirait comme « tout réglé » pour un mois sans rien à régler, ce qui n'est pas le même fait.
6. **Finition `FlowChart`** : lueur (`box-shadow`) ajoutée aux deux barres existantes, dérivée de leur propre couleur de statut (vert entrée / rouge dépense), jamais de l'accent bleu — ces barres portent un sens de statut, pas une sélection, donc la règle déjà établie du projet (ne jamais repeindre une couleur de statut avec la teinte d'accent) s'applique.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run build` (réussi), `pnpm run test` (**142/142**), les 10 scénarios `test:e2e` rejoués individuellement, tous verts (run groupé une nouvelle fois sujet au même flake connu de ce bac à sable, confirmé non régression). Captures Playwright réelles revues sur Accueil (page complète), Mes comptes, Abonnements et Épargne et projets : dégradé bleu visible sous la courbe de patrimoine, mini-courbes et pourcentages de part cohérents sur chaque carte de compte, barre « Réglé ce mois » à 0 % cohérente avec les cartes de statistiques (rien réglé sur les données de démonstration), barres de progression des objectifs à 72 %/65 % cohérentes avec les montants affichés.

Fusionné dans `main` (commit `cf7d9b9`, PR #39). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit. Dernier des 3 lots de la refonte « verre + bleu néon » — la demande initiale de l'utilisateur (transformer tout le visuel de l'app vers ce langage verre + bleu néon) est intégralement livrée.

## Correctif — intensité réelle du thème « verre + bleu néon » sur mobile, teinte assombrie, logo recoloré (corrigé et testé le 23 septembre 2026)

Après la fusion des 3 PR de la refonte, l'utilisateur a testé l'app réelle sur son iPhone (Safari, capture à l'appui) et signalé que le fond restait quasiment noir, sans le halo bleu bien visible des images de référence. Deux défauts réels distincts, tous deux invisibles dans les vérifications précédentes de cette session (faites en Chromium desktop dans ce bac à sable, jamais en conditions mobiles réelles) :

1. **Mécanisme cassé sur mobile** : le halo de fond (`body::before` au lot 1) utilisait `background-attachment: fixed` sur `body` — une propriété historiquement peu fiable sur Safari iOS. Remplacé par un pseudo-élément dédié en `position: fixed`, fiable partout y compris iOS, avec une troisième source de lumière ajoutée près du bas de page pour qu'une longue page (Mon mois) ne traverse jamais une zone plate sans glow.
2. **Intensité insuffisante même une fois le mécanisme corrigé** : capturé en conditions mobiles réelles après le fix du point 1, le halo restait à peine perceptible — les opacités vérifiées au lot 1 (`--accent-glow-ambient: 0.14`, `--glass-bg: 0.055`) étaient mathématiquement présentes mais visuellement trop discrètes sur un fond aussi sombre. `finance-designer` a repris chaque token un par un et les a intensifiés significativement (halo +71 %, verre des cartes +64 %, bordures en dégradé, glow des boutons/icônes), en recalculant par la formule de luminance relative WCAG le contraste de CHAQUE paire texte/fond réellement affectée à travers tout le fichier — `--muted`/`--subtle` ont dû être légèrement éclaircis, plusieurs sites (`.card-action`, `.text-button`, `.source-link`, `.auth-art .pill`, l'icône active de la nav) sont passés de `--accent` à `--accent-strong` (la variante texte déjà réservée à cet usage), les badges de nature de compte ont été réajustés, et `--glass-bg-row` (listes denses, déjà imbriquées dans une carte en verre) a été délibérément laissé inchangé après vérification qu'une hausse y cassait `--muted` sous 4.5:1 — cohérent avec la règle déjà établie du projet de réserver le verre le plus intense aux conteneurs, pas aux listes.

Pendant ce travail, retour supplémentaire de l'utilisateur : le bleu lui-même paraissait « trop bleu », une demande de teinte plus sombre/posée (pas plus claire, et sans annuler l'intensification en cours). `--accent` est passé de `#4f8cff` à `#397bf7` (même teinte 219°, luminosité réduite ~6 points, saturation réduite 8 points) ; `--accent-strong` a été désaturé sans être assombri, un compromis délibéré car l'assombrir de la même façon cassait le pire cas (libellé actif de la nav mobile, sous AA à 3.96:1) — ce token existe spécifiquement comme marge de sécurité pour le texte. Tous les `rgba(79, 140, 255, …)` codés en dur dans le fichier (halo, bordures, glow, badge du coffre) reprennent la nouvelle teinte `rgba(57, 123, 247, …)`.

**Logo recoloré** (`finance-identite-visuelle`, en parallèle, périmètre strictement disjoint de `index.css`) : les trois fichiers SVG (`public/finance.svg`, `favicon.svg`, `finance-maskable.svg`) étaient restés en mint depuis avant cette refonte — jamais recolorés pendant les 3 PR précédentes. Dégradé du symbole reconstruit à partir des tokens bleus déjà en place (`--accent-strong` → `--accent` → `--accent-button-from`, ce dernier préféré à `--accent-button-to` suggéré initialement car ce dernier ne mesurait que 2.95:1 contre le fond, sous le seuil non-texte de 3:1), contour du badge pivoté vers la même teinte bleue en conservant saturation/luminosité, fond neutre du badge laissé intact (déjà une teinte bleu-neutre cohérente avec `--bg`).

**Défaut hors périmètre trouvé et corrigé** (`src/App.tsx`, coordinateur) : `MONOGRAM_PALETTE` (couleurs des pastilles d'initiales pour les positions d'investissement sans compte associé) prétendait « refléter --accent et les badges » mais n'avait jamais été resynchronisé — l'entrée 0 était restée sur l'ancien mint `#7fcba8` depuis avant le lot 1. Recalculé par calcul (empilement réel : pastille sur `.item-card.row` imbriqué dans `.card`, sous le pic du halo) : les 6 teintes échouaient toutes sauf une (3.40–4.90:1, plusieurs sous AA) une fois l'intensification appliquée. Les 6 couleurs ont été éclaircies pour atteindre ≥4.5:1 dans ce pire cas réel, et le commentaire du tableau a été réécrit pour ne plus prétendre à une synchronisation qu'il ne tenait pas — exactement le type de dérive silencieuse qui a causé ce défaut.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run build` (réussi), `pnpm run test` (**142/142**), les 10 scénarios `test:e2e` rejoués individuellement, tous verts (run groupé toujours sujet au même flake connu de ce bac à sable, confirmé non régression). Aucun test n'a de dépendance sur les anciennes valeurs de `MONOGRAM_PALETTE` (vérifié par recherche). Captures Playwright réelles à 390×844 avec user-agent iPhone (pas seulement desktop, cause directe du défaut initial) sur l'écran de connexion, l'Accueil (haut et bas de page après défilement) et Mes comptes : halo bleu et verre des cartes maintenant clairement visibles, logo bleu cohérent avec le reste du thème, teinte perçue comme plus posée qu'avant ce correctif.

Fusionné dans `main` (commit `7f331c7`, PR #41). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Thème « Midnight Glass » appliqué à toute l'application (développé et testé le 24 septembre 2026)

L'utilisateur a fixé un thème à respecter à la lettre, « Midnight Glass » : noir graphite, verre fumé et lumière bleu glacier, un seul accent, pas de néons ni de couleurs partout. Il remplace entièrement « verre + bleu néon ». La spécification est désormais la référence de `.claude/skills/finance/references/design.md` (palette exacte, cartes, composition, typographie, graphiques, règle centrale) ; `identite-ui.md`, `SKILL.md`, les agents `finance-designer`/`finance-identite-visuelle`, `AGENTS.md` et `README.md` ont été alignés.

Ce qui change à l'écran :

- **Palette exacte** dans `src/index.css` : fond `#070A10`, surface `#10151E`, cartes `#1B202A`, accent unique `#A8BCE8`, texte `#F0F2F7`, secondaire `#929BAB`. Tous les vestiges bleu néon (`#397bf7`, `#4f8cff`, `rgba(57, 123, 247, …)`, glows, bordures en dégradé, boutons en dégradé) et menthe/violet sont supprimés ; les longs commentaires historiques du CSS sont remplacés par au plus une ligne.
- **Halo** bleu glacier légèrement violacé, toujours en `body::before` fixe (Safari iOS). Défaut réel trouvé en route : `body` avait un fond opaque qui recouvrait ce calque ; le fond est désormais porté par `:root` seul.
- **Cartes en verre fumé** (transparence 0,70, léger dégradé, reflet sur le bord supérieur, contour gris très discret, ombre douce, rayons 16/12/10 px). Le flou est réservé aux surfaces sous lesquelles du contenu passe réellement (barre supérieure collante, barre mobile, dialogues, panneau des mois, menus) ; seuls les dialogues et panneaux flottent.
- **Boutons petits et sombres**, légèrement encadrés ; l'action principale se repère au bleu glacier (texte, contour, voile), sans aplat vif. Taille visible 36 px (32 px compacts) avec une zone de toucher invisible de 44 px sur écran tactile. Les tuiles d'icônes, pastilles et badges ne sont plus encadrés : seul l'actionnable l'est. Le survol (légèrement plus clair) est limité aux appareils qui ont un vrai survol, pour qu'il ne reste plus collé après un toucher sur iPhone.
- **Montants** : entrées en bleu glacier précédées de « + », dépenses en blanc doux ; plus de vert/rouge sur les montants (`--negative` reste réservé aux erreurs et retards). Les classes `positive`/`negative` testées suivent toujours la nature.
- **Typographie** : pile système arrondie (`ui-rounded` puis SF Pro Rounded, Segoe UI Variable sous Windows), aucune taille sous 12 px, chiffres tabulaires. Icônes à trait constant de 1,25 px rendu quelle que soit leur taille.
- **Graphiques** : courbe de patrimoine fine en bleu glacier, aire dégradée qui s'efface, grille à 6 %, échelle min/max arrondie affichée à droite (design.md exigeait déjà une échelle) ; donut et barres Entrées/Dépenses dans la même famille bleu glacier ; mini-courbes par compte seulement à partir de 3 soldes datés passés (un point par date, dernier saisi retenu comme `latestBalance`).
- **Composition** : en-têtes de page compactés, notices de page déplacées en note de bas de page, « À votre attention » remonté dans la colonne centrale de l'Accueil, « Part réglée » intégrée comme barre sous « Réglé ce mois » (Abonnements), total de Mes comptes plus grand que chaque solde, comptes sur une colonne en tablette, lignes Investissements alignées sur celles des opérations, barre d'actions du dialogue collante sur mobile, menu « Plus » refermable par toucher extérieur ou Échap. Le bouton masquer/afficher redevient un bouton-icône œil (`aria-pressed` conservé). Les styles de mise en page en ligne ont été migrés en classes CSS.
- **Logo et icônes PWA** recolorés en bleu glacier sur graphite ; les PNG (`apple-touch-icon`, 192, 512, maskable) ont été régénérés depuis les SVG ; `theme-color` et le manifeste passent à `#070A10`. Impression : jetons et couleurs de graphique assombris pour rester lisibles sur blanc.

Contrastes calculés par formule WCAG sur la pile réellement composée (fond + halo au pic → carte → reflet → ligne) : texte secondaire ≥ 4,65:1 au pire (ligne survolée), montant d'entrée ≥ 6,37:1, texte du bouton principal 7,23:1, contour des champs 3,44:1 au pire (jeton `--line-field`).

Orchestration : trois workflows multi-agents (implémentation parallèle sur fichiers disjoints par `finance-designer`, `finance-frontend`, `finance-identite-visuelle`, `finance-coordinateur`, puis vérification indépendante à chaque round par `finance-verification` et `finance-designer` : tests, captures 390/820/1440 px, audit de contraste, critique clause par clause, revue visuelle), puis relecture et dernières corrections par le coordinateur. Un test e2e ajouté dans ce lot (« Part réglée ») a été adapté à la barre de progression (`role="progressbar"`, `aria-valuenow` 0 puis 100) ; aucune assertion préexistante n'a été affaiblie.

Preuves : `pnpm run typecheck` (0 erreur), `pnpm run test` (**142/142**), `pnpm run build` (réussi), les 10 scénarios `test:e2e` rejoués individuellement, tous verts. Captures réelles relues (émulation Chromium, pas un appareil physique) à 390×844 iPhone sur chaque page, dialogue ouvert et menu « Plus », et à 1440×900. Limites : `ui-rounded` n'existe que sur Safari (Chrome/Edge affichent une police nette non arrondie) ; le rendu du verre et du halo reste à confirmer sur iPhone réel.

Fusionné dans `main` (commit `cb555fc`, PR #43). CI (`Finance verification`) et déploiement (`Deploy Finance to GitHub Pages`) vérifiés verts sur ce commit.

## Correctif — l'app installée restait sur l'ancienne version (corrigé et testé le 24 septembre 2026)

Après la publication de « Midnight Glass », l'utilisateur voyait encore l'ancienne version sur son iPhone. Deux défauts réels dans la mise à jour de l'app installée :

1. **Aucune vérification au retour au premier plan** : une app installée qu'on rouvre depuis l'arrière-plan ne navigue pas, donc le navigateur ne revérifiait jamais `sw.js`. `main.tsx` appelle désormais `registration.update()` à chaque retour au premier plan (au plus une fois par minute).
2. **Avis de mise à jour invisible à l'écran de déverrouillage** : l'avis « Recharger » n'existait que dans l'app ouverte, alors que l'app démarre verrouillée. Sur l'écran verrouillé, rien n'est en mémoire : la nouvelle version s'applique donc d'elle-même si rien n'est saisi ; si une saisie est en cours, l'avis « Recharger » s'affiche aussi sur cet écran. Coffre ouvert : comportement inchangé (avis, jamais de rechargement silencieux).

Preuves : `typecheck` (0 erreur), `test` (**142/142**), `build`, les **11** scénarios `test:e2e` rejoués individuellement, tous verts, dont un nouveau test « PWA update: on the lock screen… » qui échoue sans la correction (vérifié en la neutralisant) et passe avec. Pour cette fois seulement, l'iPhone doit encore fermer et rouvrir l'app deux fois (l'ancienne version installée n'a pas ce correctif) ; ensuite, les versions suivantes arrivent d'elles-mêmes.

Fusionné dans `main` (commit `d36580d`, PR #45). CI et déploiement Pages vérifiés verts.

## Entrées en vert, sorties en rouge (développé et testé le 24 septembre 2026)

Demande de l'utilisateur, captures iPhone à l'appui, sur « Midnight Glass » en service : distinguer d'un coup d'œil ce qui entre (vert) de ce qui sort (rouge). Nouveau jeton `--positive` `#8FD1B5` à côté de `--negative` `#E8A3AE`, tous deux adoucis pour rester dans le thème (jamais d'aplat ni de halo). Appliqués aux montants des lignes (Mon mois, Accueil, Abonnements), à leurs flèches, aux indicateurs d'entrées/sorties (« Votre mois », les quatre indicateurs de Mon mois, « Aperçu du mois », « Payé/Reçu ce mois ») et aux barres Entrées/Dépenses ; virements, épargne et totaux mixtes restent neutres. `design.md` consigne ce choix. Contrastes calculés par formule : vert ≥ 6,73:1 et rouge ≥ 5,79:1 au pire (ligne survolée pendant le flash de règlement), 6,43:1 et 7,29:1 à l'impression.

Fusionné dans `main` (commit `20c2f30`, PR #46). CI et déploiement Pages vérifiés verts.

## Boutons, statuts, virements et épargne colorés (développé et testé le 24 septembre 2026)

Suite demandée par l'utilisateur (choix explicite parmi les pistes proposées) : boutons « Reçu » teinté vert, « Payer » teinté rouge, « Régler » (virements, épargne récurrente) teinté bleu glacier — voile léger et contour fin, jamais d'aplat vif ; statuts des lignes colorés (« Reçu » vert, « Payé » rouge, « Réglé » bleu, et ce qui reste à faire — « Pas encore payé/reçu », « Prévu », « À vérifier » — en ambre doux `--warning`) ; virements et épargne en bleu glacier (montants, flèches, tuile « Épargne (mensuel) », récurrences d'épargne). `design.md` consigne la règle. Contrastes par formule : pire cas 4,86:1 (texte du bouton « Payer » survolé), ambre 6,58:1 et bleu 6,21:1 pendant le flash de règlement.

Fusionné dans `main` (commit `f41c697`, PR #47). CI et déploiement Pages vérifiés verts.

## Correctif — avis de nouvelle version invisible, version affichée (corrigé et testé le 24 septembre 2026)

L'utilisateur ne voyait toujours pas les couleurs sur son iPhone alors que tout était fusionné et déployé. Cause trouvée : coffre ouvert, l'avis « Recharger » s'affichait dans le contenu, sous l'en-tête de page ; défilé plus bas, il était hors écran et l'app restait sur l'ancienne version. Désormais :

1. l'avis est un bandeau fixe en haut de l'écran, visible quel que soit le défilement (coffre ouvert comme verrouillé) ;
2. verrouiller le coffre applique la mise à jour en attente (plus rien n'est en mémoire) ;
3. le pied de page affiche la version (`Version <commit> du <date de build>`) pour vérifier sur l'appareil quelle version tourne.
4. défaut trouvé par la CI sur ce lot et corrigé : une mise à jour arrivant pendant l'ouverture du coffre (clé en cours de dérivation, champs déjà vidés) rechargeait la page et annulait l'ouverture. Dès qu'une saisie ou un envoi a eu lieu sur l'écran d'accès, l'app affiche désormais le bandeau au lieu de recharger.

Preuves : `typecheck`, `test` (**142/142**), `build`, les **13** scénarios `test:e2e` rejoués individuellement ; le test « PWA update: after typing on the lock screen… » échoue avec l'ancienne logique (vérifié). Le test « PWA update: a new deploy offers… » vérifie désormais, fenêtre de téléphone défilée, que « Recharger » est dans l'écran (échoue sans le bandeau fixe, vérifié) ; le nouveau test « PWA update: locking the vault applies a waiting update » échoue sans l'application au verrouillage (vérifié).

Fusionné dans `main` (commit `d153c3d`, PR #48). CI et déploiement Pages vérifiés verts.

## Logo tirelire, installation iPhone/iPad/Windows (développé et testé le 24 septembre 2026)

Logo fourni par l'utilisateur (tirelire au trait avec pièce « $ », dorée sur noir), redessiné en SVG original et décliné en bleu glacier sur le carreau graphite, à sa demande. Trois coupes : `finance.svg` (logo complet, icônes 192/512 et manifeste), `favicon.svg` (trait épais, détails retirés, lisible dès 16 px ; utilisé aussi pour la marque dans l'app, le logo complet devenant une tache sous 32 px) et `finance-maskable.svg` (zone sûre respectée : point le plus éloigné à 178 px pour 205 px autorisés). PNG régénérés depuis les SVG (`apple-touch-icon.png` 180 sans transparence, 192, 512, maskable 512). Contraste du trait mesuré sur rendu : ≥ 5,55:1 (seuil 3:1). Installation : manifeste complété (`display_override`, `orientation`, `categories`) et balises Apple/Windows (`apple-mobile-web-app-capable`, titre, `application-name`, barre d'état translucide) ; la barre supérieure et la barre latérale réservent désormais la zone de la barre d'état iOS à toutes les largeurs (iPad installé compris). Limite : aucun essai sur appareil physique ; iOS garde parfois l'ancienne icône tant que l'app n'est pas retirée puis ré-ajoutée à l'écran d'accueil.

## Répétition « Ce mois seulement / Tous les mois » à l'ajout d'une opération (développé et testé le 24 septembre 2026)

Demande de l'utilisateur : à l'ajout d'une facture ou d'un revenu (salaire…), choisir s'il ne concerne que ce mois ou tous les mois à venir. Le choix « Tous les mois » enregistre une récurrence mensuelle (même jour, nature choisie pour une dépense : charge, abonnement, épargne, autre) au lieu d'une opération isolée ; si l'état choisi est déjà « Reçu »/« Payé », l'échéance du mois est enregistrée réglée et rattachée à la récurrence, les mois suivants apparaissent « Pas encore reçu/payé ». Non proposé pour les virements, les modifications et la saisie rapide. Test e2e dédié : salaire « Tous les mois » reçu ce mois-ci, dû le mois suivant, visible dans Abonnements.

## Synchronisation entre appareils par dépôt GitHub privé (développé et testé le 24 septembre 2026)

Demande de l'utilisateur : un seul coffre partagé entre iPhone, iPad et PC Windows ; il a choisi un dépôt GitHub privé qu'il crée lui-même. Fonctionnement :

- seule l'enveloppe chiffrée du coffre (AES-GCM, clé dérivée de la phrase secrète) est envoyée à GitHub ; la phrase secrète ne quitte jamais l'appareil ; le jeton d'accès est chiffré avec la clé du coffre dans le stockage local, jamais dans les données, les exports ni les messages d'erreur ;
- refus d'un dépôt public et du dépôt de l'application ; validation stricte du propriétaire, du dépôt, du chemin et du jeton ; la politique de sécurité (CSP) n'autorise en plus que `https://api.github.com` ;
- envoi automatique après chaque enregistrement, récupération à l'ouverture, au retour au premier plan, au retour du réseau et chaque minute app ouverte ; concurrence optimiste par `sha` ;
- modifié des deux côtés : aucun écrasement silencieux, un bandeau demande quelle version garder ; une récupération ne remplace jamais une modification locale faite pendant la synchronisation, et un enregistrement attend la synchronisation en cours ;
- nouvel appareil : « Ouvrir depuis GitHub » sur l'écran d'accès, avec la même phrase secrète ; rien n'est écrit si la phrase est fausse.

La synchronisation n'est active qu'une fois configurée par l'utilisateur sur chaque appareil (dépôt privé et jeton « fine-grained » `github_pat_` limité à ce dépôt, permission Contents en lecture et écriture) ; aucune synchronisation bancaire.

Revue de sécurité indépendante (`finance-securite`) : aucune fuite de clair, de phrase secrète ni de jeton ; intégrité tenue face à un distant altéré. Un défaut bloquant et cinq à corriger, tous corrigés avant fusion :

- un dépôt contenant un autre coffre (créé séparément, autre phrase secrète, fichier illisible) n'est plus présenté comme un conflit ordinaire : bandeau distinct, rien d'écrit, remplacement seulement après double confirmation ;
- le choix d'un conflit ne s'applique qu'aux versions affichées (sinon nouveau choix) ;
- « synchronisé » n'est affiché qu'après un succès réel ;
- un enregistrement n'attend plus le réseau : le coffre refuse une écriture calculée avant un tirage (révision vérifiée juste avant l'écriture) ;
- un éditeur ouvert pendant un tirage ne réécrit pas ses anciennes valeurs (contrôle négatif e2e vérifié : le test échoue sans la garde) ;
- jetons classiques à portée large refusés ; un gros coffre inchangé n'est plus retéléchargé ; fichier distant > 25 Mo refusé ; dépôt devenu public signalé à chaque synchronisation.

Contre-vérification par le même relecteur : tous les constats corrigés, verdict « fusionnable » ; deux défauts rares restants (tirage ignoré après désactivation, tâche de synchro partie après verrouillage) corrigés dans la foulée. Le contrôle d'en-tête `x-oauth-scopes` a été retiré : non vérifiable sans le vrai GitHub et redondant avec le préfixe `github_pat_` exigé.

Preuves : `typecheck`, `test` (**178/178**, dont 64 tests de coffre et de synchronisation), `build`, les **16** scénarios `test:e2e` rejoués individuellement (dont deux scénarios à deux appareils avec un faux GitHub : chiffrement seul envoyé, ouverture depuis GitHub, tirage, conflit, choix, absence d'envoi en boucle, éditeur ouvert pendant un tirage, coffre séparé jamais écrasé sans double confirmation).

Limites : aucun essai contre le vrai GitHub (bloqué dans l'environnement de développement) ni sur appareil physique ; chaque envoi est un commit, l'historique du dépôt garde les anciennes versions chiffrées ; désactiver ne supprime ni le fichier ni le jeton (à révoquer sur GitHub).

Fusionné dans `main` (commit `fb994ff`, PR #49). CI et déploiement Pages vérifiés verts.

## Page Factures et « petit changement » d'un mois (développé et testé le 24 septembre 2026)

Demande de l'utilisateur : une page Factures pour ses factures fixes de chaque mois, visibles sur le mois en cours, avec la possibilité de modifier en cas de petit changement.

- Nouvelle page **Factures** (8ᵉ page, aussi dans la barre du bas sur téléphone : Accueil, Mon mois, Factures, Comptes, Plus) : récurrences de nature « Charge » du mois choisi, dans l'ordre des échéances (« le 5 »), avec total du mois, déjà payé, reste à payer, bouton « Payer » et « Ajouter » qui ouvre directement « Une facture ».
- **Crayon « Modifier »** (page Factures, et désormais aussi sur les échéances pas encore enregistrées de Mon mois) : montant avec le choix **« Ce mois seulement »** (enregistre l'échéance de ce mois avec son montant, les autres mois gardent le montant habituel) ou **« Ce mois et les suivants »** (nouveau montant daté du 1er du mois, mois précédents intacts), plus un lien vers la modification complète (nom, jour, compte, arrêt).
- Calcul (`finance-calculs`) : une opération liée à une échéance (montant ajusté ou règlement) remplace la projection dans la cohorte d'échéances comme dans Mon mois — mêmes montants sur toutes les pages ; `cohortSummary` accepte un filtre de nature ; `withOccurrenceAmount` enregistre le montant d'un seul mois sans doublon. Le modèle n'ayant pas de règlement partiel, une échéance réglée est close (reste dû 0).
- Démonstration : le loyer (payé) et l'assurance santé (à payer) deviennent des factures mensuelles ; totaux de Mon mois et disponible inchangés (la cohorte Abonnements et « Factures et charges » de l'Accueil les comptent désormais, comme prévu).

Vérification indépendante (`finance-verification`) : calcul principal, absence de doublon et totaux de la démonstration confirmés ; un défaut bloquant et cinq à corriger, traités ainsi :

- « Ce mois et les suivants » ne changeait rien quand on avançait la date d'un montant déjà programmé (message de succès trompeur) : corrigé dans `withRecurrenceAmount`, et « Aucun changement » est affiché quand rien ne change ;
- un mois remis au montant habituel suit de nouveau la facture (l'ajustement prévu, sans justificatif ni trace de règlement, est retiré) ;
- textes de la fenêtre exacts (un paiement déjà enregistré garde son montant ; un mois modifié seul garde le sien) ; message actionnable quand un changement plus récent existe ; « habituel … » affiché sur une échéance ajustée ; règle « un règlement lié clôt l'échéance » inscrite dans les références ;
- **défaut antérieur corrigé** : une échéance enregistrée recopiait l'identifiant d'origine Notion/import de sa récurrence, si bien que régler une récurrence importée un deuxième mois était refusé (« source en doublon ») — la provenance reste, sans cet identifiant ;
- garde par identifiant hérité alignée ; « Payer » ne réutilise jamais l'identifiant d'une autre échéance ; crayon de Mon mois vers la même fenêtre pour toute échéance de récurrence ; fenêtre fermée si une synchronisation retire l'échéance ; focus sur le montant.

Contre-vérification par le même relecteur : un défaut restait dans le parcours réel — la trace « Modification manuelle » posée par l'app empêchait le retour au montant habituel (le mois restait figé) ; corrigé (la trace n'est plus comparée), avec un test unitaire et un scénario e2e qui échouent tous deux sans la correction (vérifié). Corrigés aussi : une échéance enregistrée qui n'est plus due garde l'éditeur rapide ; le focus va vraiment sur le montant ; réenregistrer le même montant ne change rien.

Preuves : `typecheck`, `test` (**210/210**), `build`, les **17** scénarios `test:e2e` rejoués individuellement, dont « bills: … » (étendu : retour au montant habituel suivi d'un changement « et les suivants » qui l'atteint, rechargement et déverrouillage) (facture ajoutée depuis Factures, 85 ce mois seulement puis 80 le mois suivant, 90 à partir du mois suivant, mois précédent à 80, paiement du mois ajusté sans doublon dans Mon mois). Captures iPhone de la page et de la fenêtre « Modifier » revues.

## Revenus sur la page Factures (développé et testé le 24 septembre 2026)

Fusion précédente : page Factures dans `main` (commit `cfb9856`, PR #50), CI et déploiement Pages vérifiés verts.

Demande de l'utilisateur : mettre aussi les revenus sur la page Factures. La page montre désormais, pour le mois choisi, les factures (« Factures de … », « Reste à payer ») et les revenus fixes (« Revenus de … », « Reste à recevoir »), avec une barre de part payée/reçue ; une carte « Mes revenus » (salaire…) avec « Reçu » et le même crayon « Ce mois seulement / Ce mois et les suivants » ; « Ajouter un revenu » ouvre directement « Un revenu ». Calcul : `cohortSummary(…, ["income"])` compte les revenus attendus et reçus (la nature « revenu » est réservée aux revenus, validation.ts) ; sans filtre, comportement inchangé. Démonstration : le salaire devient un revenu mensuel déjà reçu.

Relecture indépendante (`finance-verification`) : fusionnable, aucun bloquant ; `cohortSummary` sans filtre inchangé sur 18 000 comparaisons aléatoires ancien/nouveau code ; totaux Factures cohérents avec Mon mois. Corrigé ensuite : assertions e2e renforcées (le bouton « Reçu »/« Payer » disparaît, « Reste à recevoir »/« Reste à payer » valent exactement 0.00 CHF, contrôle dans Mon mois et après rechargement et déverrouillage), « Factures arrêtées » sous ses factures et section « Revenus arrêtés », barres masquées en mode « montants masqués », tri stable entre devises, titre et catégorie par défaut d'un nouveau revenu.

Preuves : `typecheck`, `test` (**211/211** ; le test des revenus échoue avec l'ancien filtre, vérifié), `build`, les **18** scénarios `test:e2e` rejoués individuellement, dont « bills page also lists recurring income… » ; capture iPhone revue.

## Connexion simplifiée : Face ID, code d'ajout d'appareil, clé GitHub seule (développé et testé le 24 septembre 2026)

Fusion précédente : revenus sur la page Factures dans `main` (commit `90fbc4a`, PR #51), CI et déploiement Pages vérifiés verts.

Demande de l'utilisateur : se connecter le plus simplement possible, avec un seul compte et la même phrase secrète sur tous ses appareils, Face ID ou l'empreinte, toujours synchronisé, et une connexion GitHub sans formulaire à rallonge. Un compte serveur avec e-mail et mot de passe demanderait un service externe (exclu) : « un seul compte » = même phrase secrète + même dépôt GitHub privé.

- **Face ID / empreinte / Windows Hello** (`quickUnlock.ts`, carte « Connexion rapide » dans Documents et réglages) : activé par appareil après la phrase secrète ; la phrase, complétée à une taille fixe, est chiffrée avec une clé dérivée (HKDF) de l'extension WebAuthn PRF, rendue seulement après vérification de l'utilisateur. La demande Face ID part dès le toucher (la vérification de la phrase tourne en parallèle, pour que Safari ne refuse pas une demande jugée trop tardive). Le réglage est lié au coffre : il s'efface si un autre coffre remplace celui-ci, et reste actif après une synchronisation du même coffre. Sans PRF, la carte le dit et la phrase secrète reste le seul moyen.
- **Code d'ajout d'appareil** (`deviceLink.ts`, bouton « Ajouter un appareil » de la carte de synchronisation) : code `FIN1.…` à partager ou copier, puis à coller dans Finance sur l'autre appareil (« J'ai déjà un compte sur un autre appareil ») avec la phrase secrète. Les réglages de synchronisation (clé GitHub comprise) y sont chiffrés avec la clé du coffre. Jamais une adresse à ouvrir : elle resterait dans l'historique du navigateur.
- **Clé GitHub seule** : trois étapes (« 1. Créer le dépôt privé » et « 2. Créer la clé d'accès » ouvrent GitHub pré-rempli, « 3. Clé d'accès ») ; Finance retrouve seul l'identifiant et le dépôt (`detectSyncRepos`) mais ne choisit sans demander que le dépôt privé `finance-coffre` : un autre dépôt s'indique dans « Options avancées », une clé qui ouvre plusieurs dépôts privés est refusée.
- **Écran d'accès** : sur un nouvel appareil, « J'ai déjà un compte sur un autre appareil » (code + phrase) ; le reste (clé GitHub, restauration d'une sauvegarde, remplacer le compte) sous « Autres options » ; bouton Face ID quand il est activé ; indication d'installation (Safari → Partager → « Sur l'écran d'accueil » sur iPhone/iPad, bouton « Installer » quand le navigateur le propose) avec le rappel que chaque navigateur garde ses propres données. Sur l'Accueil, une notice « Relier mes appareils » tant que la synchronisation n'est pas configurée.

Revue de sécurité indépendante (`finance-securite`) : aucun constat bloquant ; clé envoyée seulement à `api.github.com`, dépôts publics toujours refusés, bornes du code vérifiées (19 cas), phrase jamais dans l'état React. Trois constats à corriger, tous corrigés : l'adresse d'ajout restait dans l'historique du navigateur (→ code seul à coller) ; l'ancienne phrase restait déchiffrable par Face ID après remplacement du coffre (→ réglage lié au sel du coffre) ; un dépôt privé sans rapport pouvait être choisi en silence (→ seul `finance-coffre`). Corrigés aussi : longueur de la phrase masquée, texte mentionnant le code de l'appareil, erreur de désactivation affichée. Contre-vérification par le même relecteur : correctifs confirmés par exécution (Face ID conservé après une synchronisation du même coffre, effacé après import d'un autre coffre ; aucun autre dépôt choisi en silence ; chiffré de taille fixe), verdict « fusionnable ». Son dernier point, la lecture complète du coffre avant la demande Face ID (lente sur un gros coffre), est corrigé : seul le sel est lu (`vaultSaltOnly`).

Preuves : `typecheck`, `test` (**241/241**, dont 18 tests Face ID, 6 du code d'ajout et 5 de détection du dépôt ; contrôles négatifs : chaque mutation injectée — 13 sur Face ID, 3 sur la détection, 2 sur la demande au toucher — fait échouer les tests), `build`, les **20** scénarios `test:e2e` rejoués individuellement, dont « add a device with a code… » (code créé sur A sans jeton ni nom de dépôt, phrase fausse refusée, ajout réussi sur B) et « quick unlock… » (authentificateur virtuel Chromium avec PRF : activation, verrouillage, déverrouillage sans phrase, après rechargement). Captures iPhone des écrans d'accès et des cartes revues. Un scénario de mise à jour PWA a expiré une fois en CI puis est passé sur les commits suivants ; l'arrêt du serveur de test ferme désormais ses connexions.

Limites : aucun essai sur appareil physique ni contre le vrai GitHub (bloqué dans l'environnement de développement). Face ID dans une PWA demande l'extension PRF : iOS/iPadOS 18 ou plus récent, Windows Hello seulement sur des versions récentes de Windows 11 (sources lues en extraits, non vérifiées) — sinon la phrase secrète reste nécessaire. La vérification accepte aussi le code de l'appareil. L'app installée et chaque navigateur gardent des données séparées (le code d'ajout sert aussi à relier l'app installée). Le code d'ajout se protège comme une sauvegarde : il permet d'essayer des phrases hors ligne et ne se révoque qu'en régénérant la clé GitHub. Une clé « All repositories » n'est pas détectable quand `finance-coffre` est le seul dépôt privé : l'étape 2 demande « Only select repositories ». Tous les projets Pages du compte partagent l'origine `mendestrading21.github.io` (risque préexistant).

## Factures et revenus sans date : « Tous les mois » ou « Seulement ce mois » (développé et testé le 24 septembre 2026)

Fusion précédente : connexion simplifiée dans `main` (commit `af9bf3a`, PR #52), CI et déploiement Pages vérifiés verts.

Demande de l'utilisateur, capture à l'appui (« Impôts — octobre 2026 · Aucune échéance ») : plus de dates ni d'échéances pour les factures ; à l'ajout, seulement « tous les mois » ou « juste le mois sélectionné ». Cause : l'ancien formulaire demandait jour, fréquence, début et fin ; un début par défaut « aujourd'hui » après le jour choisi sautait le mois en cours, et une fin ou une fréquence mal réglée laissait une facture sans échéance.

- **Ajout** (page Factures) : libellé, montant, devise, compte et « Répétition » : **Tous les mois** (dès le mois affiché) ou **Seulement {mois affiché}**. Aucun jour, début, fin, fréquence ni catégorie à saisir.
- **Modification** d'une facture ou d'un revenu existant : même formulaire. Le jour interne et les paiements déjà enregistrés sont conservés. Une cadence réglée ailleurs (tous les 3 mois, fin datée) reste telle quelle avec « Comme maintenant ». Un seul mois a un seul montant.
- **Page Factures** : chaque ligne indique « Tous les mois » ou « Seulement ce mois » au lieu du jour, et plus jamais « Aucune échéance ». Ce qui n'est pas dû ce mois-ci est replié dans « Factures d'autres mois » / « Revenus d'autres mois ». Tri : à payer d'abord, puis montant.
- **Mon mois** et fenêtre « Modifier » : plus de date inventée pour une facture ou un revenu pas encore réglé. Un paiement garde sa vraie date.
- Calcul : `simpleRepeatOf`, `monthBounds` et `withSimpleSchedule` (finance.ts). Ils ne déplacent jamais un début existant plus tard, sauf pour « un seul mois ». Un nouveau montant s'applique dès le mois affiché, via `withRecurrenceAmount`.

Vérification indépendante (`finance-verification`) en cours.

Preuves : `typecheck`, `test` (**250/250**, dont 9 tests « sans date » ; l'ancien défaut « début aujourd'hui = rien ce mois-ci » est reproduit dans un test), `build`, les **20** scénarios `test:e2e` rejoués individuellement. Ils couvrent notamment :
- « bills… » :
  - facture ajoutée sans date sur le mois précédent ;
  - facture « Seulement ce mois » présente ce mois, absente le mois suivant et repliée le mois d'avant ;
  - paiement des deux ;
  - passage d'un seul mois à « Tous les mois » depuis son formulaire.
- « bills page also lists recurring income… » ;
- « daily entries… » : une facture créée depuis Abonnements se rouvre en « Une facture » sans date.

Captures ordinateur et iPhone de la page, du formulaire et de Mon mois revues.

Limites :
- Les factures déjà créées avec l'ancien formulaire gardent leur jour et leurs dates internes, mais n'affichent plus de date. Une facture comme « Impôts », sans échéance ce mois-ci, apparaît dans « Factures d'autres mois », où le crayon permet de choisir « Tous les mois » ou « Seulement ce mois ».
- Réactiver « Tous les mois » sur une facture terminée depuis longtemps fait réapparaître les mois intermédiaires comme non payés.
- La page Abonnements garde son formulaire complet pour les abonnements.

## État réel

| Élément                           | État                                                                                                                                                                   | Résultat et limite                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Audit du dépôt                    | Terminé                                                                                                                                                                | Finance est une PWA React 19, TypeScript et Vite. L’ancien traqueur reste archivé dans `legacy/`. Le dépôt canonique public est `Mendestrading21/Finances`.                                                                                                                                                                                                                                                                                                                                                              |
| Audit Notion                      | Terminé pour les sources accessibles lors de l’import initial                                                                                                          | Budgets, comptes, factures, revenus, abonnements et espace Trading ont été rapprochés. Les données et ambiguïtés privées restent hors Git.                                                                                                                                                                                                                                                                                                                                                                               |
| Modèle, calculs et coffre         | Développés et testés sur la version actuelle                                                                                                                           | Montants exacts, inconnus explicites, dates distinctes, transferts neutres, coffre chiffré local, sauvegarde et restauration. Pas de serveur propre ; synchronisation entre appareils possible via un dépôt GitHub privé de l'utilisateur, seulement une fois configurée par lui.                                                                                                                                                                                                                                                                                                                          |
| Application actuelle              | Livrée : sept pages                                                                                                                                                    | Vue d’ensemble, Mon mois, Mes comptes, Abonnements, Épargne et projets, Investissements, Documents et réglages. Cartes de comptes compactes (V2.5), icônes sémantiques et monogrammes d’établissement (V2.6) livrés ; logo Finance existant vérifié conforme (V2.7).                                                                                                                                                                                                                                                                                                   |
| Amélioration V2                   | V2.1 à V2.8 développés, testés, vérifiés et fusionnés dans `main`                                                                                                      | V2.1, V2.2 (statuts, sélecteur de mois, cohorte d’échéances), V2.3 (page Abonnements), V2.4 (comptes, répartitions et tri des abonnements), V2.5 (cartes de comptes compactes), V2.6 (icônes et monogrammes), V2.7 (vérification du logo existant) et V2.8 (passe de vérification finale : typecheck/tests/build/audit/e2e/CI) livrés, voir ci-dessus. Les défauts préexistants `monthSummary` (mises de côté), débordement compte/établissement et débordement `.review-item` sont corrigés, voir sections dédiées ci-dessus. La PR #5 est fusionnée (commit `12f1d1d`) ; de nombreux lots ultérieurs (voir « Prochaine action » pour la liste complète) sont fusionnés depuis, dont la dernière refonte visuelle (commit `c4f9302`). |
| Skill et agents                   | Livrés                                                                                                                                                                 | Skill maître Finance, huit références et onze missions spécialisées, dont Abonnements et Identité visuelle.                                                                                                                                                                                                                                                                                                                                                                                                              |
| Publication GitHub                | À jour, aucune PR de fonctionnalité en attente                                                                                                                        | Branche `claude/finance-app-completion-k86h7n`, alignée sur `main` du dépôt public `Mendestrading21/Finances` au commit `3065574` (dernier lot fusionné : documentation de la refonte visuelle, PR #29). Publication continue au fil des lots, chacun vérifié (CI + Pages) avant la suite.                                                                                                                                                                                                                             |
| GitHub Pages                      | Workflow vérifié ; site public à jour                                                                                                                                 | Site public disponible sur `https://mendestrading21.github.io/Finances/`, reflète `main` à son dernier commit fusionné (`3065574`, vérifié vert). Le workflow Pages accepte les publications de `main` et un lancement manuel ; recontrôlé après chaque fusion.                                                                                                                                                                                                                                                        |
| Appareils physiques               | Non testé                                                                                                                                                              | Les rendus Chromium 390 px, 834 px et ordinateur existent ; aucun essai physique Safari iPhone/iPad ou Edge Windows n’est prouvé.                                                                                                                                                                                                                                                                                                                                                                                        |
| Synchronisation bancaire ou cloud | Entre appareils : développée ; bancaire : non | Entre appareils : dépôt GitHub privé choisi par l’utilisateur, enveloppe chiffrée seulement, conflits résolus par choix explicite (section dédiée ci-dessus) ; inactive tant qu’elle n’est pas configurée sur chaque appareil. Aucune connexion bancaire. |

## Vérité financière

La correspondance publique est dans [CORRESPONDANCE_NOTION.md](CORRESPONDANCE_NOTION.md). L’import conserve source et date dans le fichier privé. Un mois budgétaire n’invente pas une date de règlement. Les soldes sans date, règlements ambigus et détentions non prouvées ne deviennent pas des valeurs actuelles.

La V2 devra distinguer la **cohorte d’échéances** et le **flux réalisé**. Une charge due en février et payée en mars est soldée dans la cohorte de février, mais son débit appartient au flux de mars. Les transferts internes restent neutres et un compte d’investissement ne s’additionne pas à ses positions lorsqu’ils représentent la même valeur.

Le rapprochement des données personnelles reste nécessaire avant d’affirmer un patrimoine complet : confirmer les soldes datés, règlements, comptes de destination, récurrences et positions détenues. Une information importée puis modifiée garde une indication de modification manuelle.

## Preuves vérifiées avant la tranche V2

| Contrôle                                          | Résultat observé                                                                                                                                                                    |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Référence applicative                             | `main` au commit `c305f89348acf7d854515efc60e9e377ca5f583d` avant la publication de ce skill V2.                                                                                    |
| CI applicative                                    | Workflow `Finance verification` 35316300782 terminé avec succès sur cette référence.                                                                                                |
| GitHub Pages                                      | Workflow 35317840296 terminé avec succès ; site public ouvert et démonstration affichée.                                                                                            |
| Validation du skill V2                            | Validateur officiel `quick_validate.py` réussi ; liens locaux et format Prettier contrôlés.                                                                                         |
| Relecture V2                                      | Forward-test indépendant effectué sur migration, occurrence, mois de règlement, tri multidevise, logos et ordre des lots ; ambiguïtés corrigées avant publication.                  |
| V2.1 livré                                        | PR #5 vers `main`, contrôles locaux détaillés dans la section « Lot V2.1 » ci-dessus.                                                                                                |
| V2.2 (complet) livré                              | Même PR #5, mêmes contrôles, relu indépendamment ; détail dans la section « Lot V2.2 » ci-dessus.                                                                                   |
| V2.3 (page Abonnements) livré                     | Même PR #5, mêmes contrôles, relu indépendamment en deux passes ; détail dans la section « Lot V2.3 » ci-dessus.                                                                    |
| V2.4 (comptes, répartitions et abonnements) livré | Même PR #5, mêmes contrôles, relu indépendamment ; détail dans la section « Lot V2.4 » ci-dessus.                                                                                   |
| Correctif `monthSummary` (mises de côté) livré    | Même PR #5, mêmes contrôles, relu indépendamment avec contrôle négatif et scénario navigateur ; détail dans la section « Correctif — mises de côté dans `monthSummary` » ci-dessus. |
| V2.5 (cartes de comptes compactes) livré          | Même PR #5, mêmes contrôles, relu indépendamment avec mesure réelle propre au relecteur (323→271px, 3/6→6/6 comptes visibles) ; détail dans la section « Lot V2.5 » ci-dessus.      |
| Correctif débordement compte/établissement livré  | Même PR #5, mêmes contrôles, test e2e dédié avec contrôle négatif ; détail dans la section dédiée ci-dessus.                                                                        |
| V2.6 (icônes et monogrammes) livré                | Même PR #5, mêmes contrôles ; détail dans la section « Lot V2.6 » ci-dessus.                                                                                                         |
| V2.7 (vérification du logo existant) livré        | Même PR #5, mêmes contrôles, relu indépendamment (trois revues : correctif débordement, V2.6, vérification V2.7) ; détail dans la section « Lot V2.7 » ci-dessus.                   |
| Correctif débordement `.review-item p` livré      | Même PR #5, mêmes contrôles, test e2e dédié avec double contrôle négatif (mot allongé puis `git stash`/rebuild) ; détail dans la section dédiée ci-dessus.                          |
| V2.8 (livraison finale) livré et fusionné         | PR #5 fusionnée dans `main` (commit `12f1d1d`) ; CI et GitHub Pages verts sur ce commit, site public à jour ; détail dans la section « Lot V2.8 » ci-dessus.                        |
| Correctif service worker PWA livré et fusionné    | PR #6 fusionnée dans `main` (commit `a466623`) ; CI et GitHub Pages verts sur ce commit ; relu indépendamment avec deux défauts réels trouvés et corrigés ; détail dans la section « Correctif — mise à jour du service worker PWA » ci-dessus. |
| Correctif regroupement des actions de ligne livré et fusionné | PR #7 fusionnée dans `main` (commit `de04fb4`) ; CI et GitHub Pages vérifiés verts sur ce commit ; développé par `finance-designer`, relu indépendamment par `finance-verification` (un défaut réel trouvé et corrigé, puis revérifié par le coordinateur) ; détail dans la section dédiée ci-dessus. |
| Carte « Le mouvement du mois » sur l'Accueil livrée et fusionnée | PR #8 fusionnée dans `main` (commit `525ee77`) ; CI et GitHub Pages vérifiés verts sur ce commit ; relu indépendamment par `finance-verification`, aucun défaut trouvé ; détail dans la section dédiée ci-dessus. |
| Section « Aperçu du mois » sur l'Accueil livrée et fusionnée | PR #9 fusionnée dans `main` (commit `e51f39a`) ; CI et GitHub Pages vérifiés verts sur ce commit ; relu indépendamment par `finance-verification` avec reproduction indépendante (plusieurs devises, cadences et un cas de taux manquant), aucun défaut trouvé ; détail dans la section dédiée ci-dessus. |
| Badges de nature de compte livrés et fusionnés | PR #10 fusionnée dans `main` (commit `45c59c8`) ; CI et GitHub Pages vérifiés verts sur ce commit ; développé par `finance-designer` (sans commit, consigne explicite), relu indépendamment par `finance-verification` (un défaut réel de contraste trouvé et corrigé par le relecteur, revérifié par le coordinateur) ; détail dans la section dédiée ci-dessus. |
| Page « Mes comptes » en vraie table livrée et fusionnée | PR #11 fusionnée dans `main` (commit `696018d`) ; CI et GitHub Pages vérifiés verts sur ce commit ; détail dans la section dédiée ci-dessus. |
| Retour aux cartes + refonte visuelle (icônes, cartes, listes) livrée et fusionnée | PR #12 fusionnée dans `main` (commit `4fb4e3c`) ; CI et GitHub Pages vérifiés verts sur ce commit ; trois passes développées (`finance-designer`, `finance-identite-visuelle`, coordinateur), relues indépendamment par `finance-verification`, aucun défaut bloquant (un point de cohérence documentaire corrigé) ; détail dans la section dédiée ci-dessus. |
| Tri « pas payé d'abord » + « Voir les autres mois » (Mon mois) livré et fusionné | PR #13 fusionnée dans `main` (commit `91ad711`) ; CI et GitHub Pages vérifiés verts sur ce commit ; détail dans la section dédiée ci-dessus. |
| Simplification des lignes réglées (Mon mois) + audit qualité (contraste, cibles tactiles, focus clavier, CSS mort) livrée et fusionnée | PR #14 fusionnée dans `main` (commit `5b9bf29`) ; CI et GitHub Pages vérifiés verts sur ce commit ; développé par le coordinateur et relu indépendamment par `finance-verification` en deux passes (un défaut réel trouvé et corrigé à chaque passe) ; détail dans les deux sections dédiées ci-dessus. |
| Paiement instantané, édition rapide, simplification Abonnements/Mon mois livrée et fusionnée | PR #16 fusionnée dans `main` (commit `249c009`) ; CI et GitHub Pages vérifiés verts sur ce commit ; développé par le coordinateur et trois agents en parallèle sur des fichiers disjoints, relu indépendamment par `finance-verification` (trois défauts réels trouvés et corrigés, revérifiés) ; détail dans la section dédiée ci-dessus. |
| Audit à sept agents (sécurité, calculs, patrimoine, design, import/migration, éditeur, couverture e2e) livré et fusionné | PR #18 fusionnée dans `main` (commit `04ad766`) ; CI et GitHub Pages vérifiés verts sur ce commit ; sept agents sur des fichiers strictement disjoints, coordinateur sur `src/App.tsx` ; trois défauts réels trouvés et corrigés (historique de montant de récurrence, contraste du bouton principal, flash vert d'Abonnements structurellement inatteignable — ce dernier trouvé par le coordinateur en vérifiant le test ajouté par `finance-verification`) ; détail dans la section dédiée ci-dessus. |

Ces preuves n’attestent pas encore l’implémentation de la V2 au-delà de V2.1. Les totaux de tests, SHA et exécutions doivent être relus après chaque nouveau changement. La [revue indépendante](REVUE_INDEPENDANTE.md) décrit les contrôles de l’application initiale et leurs limites.

## Captures existantes

| Capture                                             | Ce qu’elle prouve                  |
| --------------------------------------------------- | ---------------------------------- |
| [00 — Existant](captures/00-existant-habitudes.png) | Traqueur initial avant Finance.    |
| [01 — Coffre](captures/01-coffre-desktop.png)       | Création réelle de l’espace privé. |
| [02 — Ordinateur](captures/02-finance-desktop.png)  | Tableau de bord à 1 440 px.        |
| [03 — Tablette](captures/03-finance-ipad.png)       | Tableau de bord à 834 px.          |
| [04 — Téléphone](captures/04-finance-iphone.png)    | Tableau de bord à 390 px.          |
| [05 — Projets](captures/05-projets-iphone.png)      | Épargne et projets sur téléphone.  |

Toutes les captures utilisent la démonstration fictive. Elles ne prouvent ni Safari/WebKit ni un appareil physique. Chaque lot visuel V2 devra ajouter ses propres captures après développement.

## Prochaine action

V2.1 à V2.8, les correctifs PWA/actions de ligne, la carte « Le mouvement du mois », la section « Aperçu du mois », les badges de nature de compte, la table « Mes comptes », la refonte visuelle générale (retour aux cartes), le tri « pas payé d'abord » + « Voir les autres mois », la simplification des lignes réglées + l'audit qualité, le paiement instantané/éditeur rapide/simplification Abonnements-Mon mois, l'audit à sept agents (sécurité, calculs, patrimoine, design, import/migration, éditeur, couverture e2e), les dates lisibles du graphique de patrimoine + accent des onglets de filtre, les coins plus carrés dans toute l'app, les boutons payer/reçu colorés + cartes Abonnements/Investissements + refonte du sélecteur de mois + palette bleu néon, le correctif du focus clavier perdu après un règlement instantané, la refonte visuelle plate et neutre qui abandonne ce dégradé bleu-violet néon (fonds de carte aplatis, accent menthe unique, logo recoloré), le correctif du bouton d'action désaligné + tri des opérations par nature et montant (Mon mois), le montant coloré par nature (rouge dépense/vert revenu) sur toutes les listes d'opérations, le lot 1/3 de la refonte visuelle « verre + bleu néon » (tokens, boutons, navigation, écran de connexion), le lot 2/3 (cartes, lignes, formulaires, glow des icônes sur les 7 pages, correctif de collision de teinte bancaire, nouvel interrupteur masquer/afficher), le lot 3/3 (aire en dégradé sous la courbe de patrimoine, mini-courbes et part du patrimoine par compte, barre « réglé ce mois » sur Abonnements, progression des objectifs et finition FlowChart en dégradé lumineux), le correctif d'intensité réelle sur mobile (mécanisme du halo réparé pour Safari iOS, halo/verre/bordures/glow intensifiés et recalculés par formule, teinte assombrie sur retour utilisateur, logo recoloré en bleu, `MONOGRAM_PALETTE` resynchronisé) sont livrés, fusionnés dans `main` (commits `12f1d1d`, `a466623`, `de04fb4`, `525ee77`, `e51f39a`, `45c59c8`, `696018d`, `4fb4e3c`, `91ad711`, `5b9bf29`, `249c009`, `04ad766`, `b47b1e3`, `14eccee`, `b5b8e74`, `e75716d`, `c4f9302`, `e1e45d1`, `538171a`, `c592af5`, `c329771`, `cf7d9b9`, `7f331c7`) et déployés sur le site public. Le thème « Midnight Glass » fixé ensuite par l'utilisateur remplace entièrement « verre + bleu néon » (section dédiée ci-dessus, fusionné au commit `cb555fc` et déployé) ; c'est désormais le thème de référence de `design.md`. Un élément vu sur une capture Notion de référence (table mensuelle « Impôt ») reste hors périmètre : aucune donnée fiscale ni modèle correspondant n'existe dans Finance, et en ajouter un exigerait d'inventer une structure sans demande explicite ni source. Reste à faire :

1. Le focus clavier après « Payer »/« Reçu »/« Régler » est corrigé (section « Correctif — focus clavier perdu après un règlement instantané » ci-dessus) : la ligne est re-ciblée par son identifiant de transaction, plus de perte de focus vers `<body>`.
2. Aucune autre piste engagée sans demande explicite de l'utilisateur : ce fil a déjà connu plusieurs itérations visuelles non concluantes avant que la dernière passe soit acceptée — mieux vaut confirmer à chaque lot plutôt que d'enchaîner sans retour.

Chaque lot est terminé avec tests ciblés, parcours navigateur, captures fictives et relecture indépendante vérifiés ; la CI distante et le site publié sont recontrôlés après chaque fusion.

Lire [DEMARRER_CLAUDE.md](../DEMARRER_CLAUDE.md) pour lancer Claude. Une nouvelle session commence par le dépôt et les accès réels ; elle ne reprend jamais un état historique comme preuve actuelle.
